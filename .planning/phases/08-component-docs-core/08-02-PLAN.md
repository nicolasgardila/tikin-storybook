---
phase: 08-component-docs-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - content/docs/components/toggle.mdx
  - content/docs/components/badge.mdx
  - content/docs/components/status-chips.mdx
autonomous: true
must_haves:
  truths:
    - "Toggle page has live preview with on/off states"
    - "Badge page has live preview with color variants"
    - "Status Chips page has live preview with 40 variant examples"
    - "Each page has ComponentPreview, code examples, PropsTable, and DoDont"
  artifacts:
    - path: "content/docs/components/toggle.mdx"
      provides: "Toggle documentation with live previews"
      min_lines: 80
      contains: "ComponentPreview"
    - path: "content/docs/components/badge.mdx"
      provides: "Badge documentation with live previews"
      min_lines: 60
      contains: "ComponentPreview"
    - path: "content/docs/components/status-chips.mdx"
      provides: "Status Chips documentation with live previews"
      min_lines: 80
      contains: "ComponentPreview"
  key_links: []
---

<objective>
Document Toggle, Badge, and Status Chips components with live previews, code examples, and props tables.

Purpose: Create documentation for small interactive and display indicator components.
Output: Three MDX component pages following the established template pattern.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@content/docs/components/buttons.mdx
@app/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Toggle and Badge component pages</name>
  <files>content/docs/components/toggle.mdx, content/docs/components/badge.mdx</files>
  <action>
    Create two new component documentation pages. Read buttons.mdx first for template pattern.

    **toggle.mdx**:
    ```
    ---
    title: Toggle
    description: Switch de activacion/desactivacion con estados on/off
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro
    2. **Off State** with ComponentPreview:
       - Track: 44px × 24px, rounded-pill (12px radius), bg #DEDEDE
       - Thumb: 20px × 20px circle, bg white, positioned left
       - Use inline styles with transition
    3. **On State** with ComponentPreview:
       - Track: bg #0B0B0B (brand)
       - Thumb: positioned right
    4. **With Label** variant — toggle + label text
    5. **Disabled** variant — muted colors
    6. Code blocks for each
    7. PropsTable: checked, disabled, label, onChange, size
    8. DoDont:
       - Do: Use for binary on/off settings
       - Don't: Use for actions that require confirmation (use button instead)

    Key specs:
    - Track: 44×24px, r12, bg #DEDEDE (off) / #0B0B0B (on)
    - Thumb: 20×20px circle, bg white, 2px inset from track edge
    - Disabled: opacity 0.5
    - Transition: 200ms ease

    **badge.mdx**:
    ```
    ---
    title: Badge
    description: Indicador de notificacion o conteo
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro
    2. **Numeric Badge** with ComponentPreview — small circle/pill with number
       - Style: min-width 20px, height 20px, rounded-full, bg #E74C3C (red), text white, font-size 11px
    3. **Dot Badge** variant — small red dot (8×8px)
    4. **On Icon** variant — badge positioned on an icon (bell icon + badge)
    5. Code blocks
    6. PropsTable: count, max, showZero, variant (dot/count), color
    7. DoDont:
       - Do: Use for notification counts, unread indicators
       - Don't: Use for static labels (use Status Chips instead)

    Content in Spanish. All ComponentPreview content uses inline styles.
  </action>
  <verify>
    - `npm run build` succeeds
    - toggle.mdx and badge.mdx exist with ComponentPreview
  </verify>
  <done>Toggle and Badge pages created with live previews, code examples, PropsTable</done>
</task>

<task type="auto">
  <name>Task 2: Create Status Chips component page</name>
  <files>content/docs/components/status-chips.mdx</files>
  <action>
    Create Status Chips documentation. This component has 40 variants in the DS (various colors and states).

    ```
    ---
    title: Status Chips
    description: Etiquetas de estado con 40 variantes de color y estilo
    status: stable
    ---
    ```

    Content:
    1. StatusBadge + intro — explain the chip system (used for transaction status, account states, etc.)
    2. **Success Chip** with ComponentPreview:
       - Style: rounded-pill (r44), px 12px, py 4px, font-size 12px, Ubuntu Sans Mono, uppercase, tracking 0.6px
       - Colors: bg #ECFDF5, text #059669, border 1px #A7F3D0
    3. **Warning Chip** with ComponentPreview:
       - Colors: bg #FFFBEB, text #D97706, border 1px #FDE68A
    4. **Error Chip**:
       - Colors: bg #FEF2F2, text #DC2626, border 1px #FECACA
    5. **Info Chip**:
       - Colors: bg #EFF6FF, text #2563EB, border 1px #BFDBFE
    6. **Neutral Chip**:
       - Colors: bg #F6F6F6, text #666666, border 1px #DEDEDE
    7. **Brand Chip** (solid):
       - Colors: bg #0B0B0B, text #FFFFFF, no border
    8. Code blocks for each variant
    9. PropsTable: variant (success/warning/error/info/neutral/brand), label, size, icon
    10. DoDont:
        - Do: Use for status indicators (Completado, Pendiente, Rechazado)
        - Don't: Use for actions (use Buttons instead)

    Key specs from DS:
    - Border radius: 44px (pill)
    - Font: Ubuntu Sans Mono, 12px, uppercase, tracking 0.6px
    - Height: ~28px (xs height token)

    Content in Spanish. Inline styles for ComponentPreview.
  </action>
  <verify>
    - `npm run build` succeeds
    - status-chips.mdx exists with >80 lines and ComponentPreview
  </verify>
  <done>Status Chips page created with 6 variant previews, code examples, PropsTable</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Toggle, Badge, Status Chips pages created
- [ ] All have ComponentPreview, code blocks, PropsTable, DoDont
</verification>

<success_criteria>

- All tasks completed
- Three small component pages documented
- Build succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/08-component-docs-core/08-02-SUMMARY.md`
</output>
