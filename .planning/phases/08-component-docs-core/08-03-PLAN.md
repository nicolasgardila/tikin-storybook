---
phase: 08-component-docs-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - content/docs/components/card.mdx
  - content/docs/components/container.mdx
autonomous: true
must_haves:
  truths:
    - "Card page has live previews showing warm/neutral/filled/elevated variants"
    - "Container page has live previews showing the double-layer pattern"
    - "Each page has ComponentPreview, code examples, PropsTable, and DoDont"
  artifacts:
    - path: "content/docs/components/card.mdx"
      provides: "Card documentation with variant previews"
      min_lines: 100
      contains: "ComponentPreview"
    - path: "content/docs/components/container.mdx"
      provides: "Container documentation with double-layer previews"
      min_lines: 80
      contains: "ComponentPreview"
  key_links: []
---

<objective>
Document Card and Container components with live previews showcasing warm/neutral tones and the double-layer pattern.

Purpose: Document the foundational layout components that demonstrate the DS's distinctive visual patterns.
Output: Two MDX pages with live previews showing the characteristic warm/neutral dual-tone system.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@content/docs/components/buttons.mdx
@app/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Card component page</name>
  <files>content/docs/components/card.mdx</files>
  <action>
    Create Card component documentation. Read buttons.mdx for template pattern.

    ```
    ---
    title: Card
    description: Tarjeta con variantes warm, neutral, filled, elevated y outline
    status: stable
    ---
    ```

    Content:
    1. StatusBadge + intro — explain card variants and warm vs neutral tones
    2. **Default (Warm)** with ComponentPreview:
       - Style: r24, border 0.5px #E8E4DD, bg #FFFFFF, padding 20px
       - Content: title (Funnel Display) + description text (Ubuntu Sans)
       - Show a realistic card with "Cuenta de Ahorro" title and balance info
    3. **Neutral** variant:
       - Border: #DEDEDE instead of warm
    4. **Filled** variant:
       - Background: #F1EFEB (warm surface), border #E8E4DD
    5. **Elevated** variant:
       - No border, shadow: 0 0 2px rgba(11,15,16,0.12)
    6. **Outline** variant:
       - Transparent bg, border 0.5px #DEDEDE
    7. Code blocks for each variant
    8. PropsTable: variant (default/neutral/filled/elevated/outline), padding (compact/default), children
    9. DoDont:
       - Do: Use warm cards for wallet/financial content, neutral for generic UI
       - Don't: Mix warm and neutral cards in the same container

    Key specs from HTML source (card.html):
    - Border radius: 24px
    - Border: 0.5px solid
    - Warm border: #E8E4DD / Dark: #2A2826
    - Neutral border: #DEDEDE / Dark: #333333
    - Body padding: 20px (default), 16px (compact)
    - Body gap: 16px (default), 12px (compact)

    Content in Spanish. Inline styles for ComponentPreview.
  </action>
  <verify>
    - `npm run build` succeeds
    - card.mdx exists with >100 lines and ComponentPreview
  </verify>
  <done>Card page created with 5 variant previews showing warm/neutral dual-tone system</done>
</task>

<task type="auto">
  <name>Task 2: Create Container component page</name>
  <files>content/docs/components/container.mdx</files>
  <action>
    Create Container component documentation. This showcases the distinctive double-layer pattern.

    ```
    ---
    title: Container
    description: Contenedor double-layer con shell exterior y panel interior
    status: stable
    ---
    ```

    Content:
    1. StatusBadge + intro — explain the double-layer container pattern (signature DS pattern)
    2. **Double-Layer Container** with ComponentPreview:
       - Outer shell: bg #F6F6F6, border 1px #DEDEDE, r24, padding 12px
       - Inner panel: bg #FFFFFF, r20
       - Show with sample content inside inner panel
       - This is the pattern used for Dialogs, Panels, complex containers
    3. **Simple Container** variant:
       - Single layer with padding, no double structure
    4. **Warm Container** variant:
       - Outer: bg #F1EFEB, border #E8E4DD
       - Inner: bg #FFFFFF
    5. Code blocks showing the nesting pattern:
       ```tsx
       <div className="rounded-[24px] border border-[#DEDEDE] bg-[#F6F6F6] p-3">
         <div className="rounded-[20px] bg-white p-6">
           {children}
         </div>
       </div>
       ```
    6. PropsTable: variant (double/simple/warm), padding, children
    7. DoDont:
       - Do: Use double-layer for dialogs, panels, complex content areas
       - Don't: Nest double-layer containers (creates too much visual depth)

    Key specs from modal.html:
    - Outer: #F6F6F6, border 1px #DEDEDE, r24, p12
    - Inner: #FFFFFF, r20, p36 (dialog) or p24 (panel)
    - Shadow: 0 0 2px rgba(11,15,16,0.12)
    - Dark: outer #171717 border #2A2A2A, inner #1E1E1E

    Content in Spanish. Inline styles for ComponentPreview.
  </action>
  <verify>
    - `npm run build` succeeds
    - container.mdx exists with >80 lines and ComponentPreview
  </verify>
  <done>Container page created with double-layer and simple variant previews</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Card and Container pages created
- [ ] Both demonstrate warm/neutral tone system visually
- [ ] Both have ComponentPreview, code blocks, PropsTable, DoDont
</verification>

<success_criteria>

- All tasks completed
- Card and Container components documented with variant previews
- Build succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/08-component-docs-core/08-03-SUMMARY.md`
</output>
