---
phase: 08-component-docs-core
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - content/docs/components/dialog.mdx
  - content/docs/components/toast.mdx
  - content/docs/components/dropdown.mdx
autonomous: true
must_haves:
  truths:
    - "Dialog page has live preview showing the double-layer pattern with backdrop"
    - "Toast page has live preview showing mobile and desktop variants"
    - "Dropdown page has live preview showing trigger + menu pattern"
    - "Each page has ComponentPreview, code examples, PropsTable, and DoDont"
  artifacts:
    - path: "content/docs/components/dialog.mdx"
      provides: "Dialog documentation with double-layer preview"
      min_lines: 80
      contains: "ComponentPreview"
    - path: "content/docs/components/toast.mdx"
      provides: "Toast documentation with mobile/desktop variants"
      min_lines: 60
      contains: "ComponentPreview"
    - path: "content/docs/components/dropdown.mdx"
      provides: "Dropdown documentation with menu preview"
      min_lines: 60
      contains: "ComponentPreview"
  key_links: []
---

<objective>
Document Dialog, Toast, and Dropdown components with live previews, then update meta.json for all new component pages.

Purpose: Document overlay/popup components and ensure sidebar navigation includes all Phase 8 components.
Output: Three MDX component pages plus updated meta.json.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@content/docs/components/buttons.mdx
@content/docs/components/meta.json
@app/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dialog and Toast component pages</name>
  <files>content/docs/components/dialog.mdx, content/docs/components/toast.mdx</files>
  <action>
    Create two overlay component documentation pages. Read buttons.mdx for template pattern.

    **dialog.mdx**:
    ```
    ---
    title: Dialog
    description: Modal con patron double-layer, backdrop y variantes de contenido
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro — explain Dialog uses the double-layer pattern
    2. **Confirmation Dialog** with ComponentPreview:
       - Show the full double-layer structure inside the preview
       - Outer shell: bg #F6F6F6, border 1px #DEDEDE, r24, p12
       - Inner panel: bg #FFFFFF, r20, p36
       - Content: title (Funnel Display "Confirmar accion"), body text, two buttons (Primary + Secondary)
       - Note: ComponentPreview simulates the dialog without actual backdrop
    3. **With Icon** variant — dialog with a warning/info icon above title
    4. Code block showing the double-layer nesting pattern
    5. PropsTable: open, onClose, title, children, size (sm/md/lg), showCloseButton
    6. DoDont:
       - Do: Use for confirmations, destructive action warnings
       - Don't: Use for displaying long content (use Side Panel instead)

    Key specs from modal.html:
    - Outer: #F6F6F6, border 1px #DEDEDE, r24, p12, shadow 0 0 2px rgba(11,15,16,0.12)
    - Inner: #FFFFFF, r20, p36
    - Backdrop: rgba(0,0,0,0.40)
    - Title: Funnel Display, centered or left-aligned
    - Dark: outer #171717 border #2A2A2A, inner #1E1E1E

    **toast.mdx**:
    ```
    ---
    title: Toast
    description: Notificacion temporal con variantes mobile y desktop
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro — auto-dismissing notification
    2. **Desktop Toast** with ComponentPreview:
       - Position: floating, right-aligned
       - Style: bg #0B0B0B, text white, r12, px 16px, py 12px
       - Content: icon + message text + optional close button
       - Shadow: md level
    3. **Mobile Toast** with ComponentPreview:
       - Position: bottom of viewport, full width with margins
       - Same style but wider
    4. **Success/Error/Warning** variants — different icon + accent color
    5. Code blocks
    6. PropsTable: message, variant (info/success/error/warning), duration, position (top-right/bottom-center), onClose
    7. DoDont:
       - Do: Use for non-critical feedback (saved, copied, sent)
       - Don't: Use for errors that require user action (use Dialog instead)

    Key specs:
    - Desktop: fixed top-right, max-width 360px
    - Mobile: fixed bottom, full-width with 16px margins
    - Background: #0B0B0B (dark toast on light bg)
    - Text: white, Ubuntu Sans 14px
    - Duration: 3-5 seconds auto-dismiss
    - Border radius: 12px

    Content in Spanish. Inline styles for ComponentPreview.
  </action>
  <verify>
    - `npm run build` succeeds
    - dialog.mdx and toast.mdx exist with ComponentPreview
  </verify>
  <done>Dialog and Toast pages created with variant previews</done>
</task>

<task type="auto">
  <name>Task 2: Create Dropdown page and update meta.json</name>
  <files>content/docs/components/dropdown.mdx, content/docs/components/meta.json</files>
  <action>
    Create Dropdown documentation and update meta.json with all Phase 8 components.

    **dropdown.mdx**:
    ```
    ---
    title: Dropdown
    description: Menu desplegable con opciones seleccionables
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro
    2. **Default Dropdown** with ComponentPreview:
       - Show a trigger button + open menu below it
       - Menu: bg white, border 1px #DEDEDE, r12, shadow-md, py 4px
       - Menu items: py 8px, px 12px, hover bg #F6F6F6
       - Since ComponentPreview renders static content, show the menu in "open" state
    3. **With Icons** variant — menu items with leading icons
    4. **With Divider** variant — sections separated by thin line
    5. Code blocks
    6. PropsTable: trigger, items, onSelect, align (left/right), width
    7. DoDont:
       - Do: Use for action menus, option lists
       - Don't: Use for primary navigation (use sidebar/tabs instead)

    Key specs:
    - Menu: bg #FFFFFF, border 1px #DEDEDE, r12
    - Shadow: 0 4px 8px rgba(0,0,0,0.08) (--ds-shadow-md)
    - Item hover: bg #F6F6F6
    - Item padding: 8px 12px
    - Font: Ubuntu Sans, 14px

    **Update meta.json** to include ALL component pages from Phase 8:
    ```json
    {
      "title": "Components",
      "defaultOpen": false,
      "pages": [
        "index",
        "buttons",
        "text-field",
        "select",
        "checkbox",
        "toggle",
        "card",
        "container",
        "badge",
        "status-chips",
        "toast",
        "dialog",
        "dropdown"
      ]
    }
    ```

    Content in Spanish. Inline styles for ComponentPreview.
  </action>
  <verify>
    - `npm run build` succeeds
    - dropdown.mdx exists with ComponentPreview
    - meta.json lists all 13 entries (index + 12 component pages)
  </verify>
  <done>Dropdown page created, meta.json updated with all Phase 8 component pages</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Dialog, Toast, Dropdown pages created
- [ ] meta.json lists all component pages
- [ ] All pages have ComponentPreview, code blocks, PropsTable, DoDont
</verification>

<success_criteria>

- All tasks completed
- Overlay/popup components documented
- Sidebar navigation shows all component pages
- Build succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/08-component-docs-core/08-04-SUMMARY.md`
</output>
