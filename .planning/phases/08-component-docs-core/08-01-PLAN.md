---
phase: 08-component-docs-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/docs/components/text-field.mdx
  - content/docs/components/select.mdx
  - content/docs/components/checkbox.mdx
autonomous: true
must_haves:
  truths:
    - "TextField page has live preview with ComponentPreview, code examples, and PropsTable"
    - "Select page has live preview with ComponentPreview, code examples, and PropsTable"
    - "Checkbox page has live preview with ComponentPreview, code examples, and PropsTable"
    - "Each page shows variants via ComponentPreview with desktop/mobile and light/dark toggles"
  artifacts:
    - path: "content/docs/components/text-field.mdx"
      provides: "TextField documentation with live previews"
      min_lines: 80
      contains: "ComponentPreview"
    - path: "content/docs/components/select.mdx"
      provides: "Select documentation with live previews"
      min_lines: 80
      contains: "ComponentPreview"
    - path: "content/docs/components/checkbox.mdx"
      provides: "Checkbox documentation with live previews"
      min_lines: 80
      contains: "ComponentPreview"
  key_links: []
---

<objective>
Document TextField, Select, and Checkbox components with live previews, code examples, and props tables.

Purpose: Create comprehensive documentation for core form input components.
Output: Three MDX component pages following the established Buttons page template pattern.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@content/docs/components/buttons.mdx
@content/docs/components/text-field.mdx
@app/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document TextField component</name>
  <files>content/docs/components/text-field.mdx</files>
  <action>
    Replace the placeholder content in text-field.mdx with full documentation following the Buttons page pattern.

    Use the Buttons page (content/docs/components/buttons.mdx) as the template — read it first.

    Content structure:
    1. Frontmatter: title "TextField", description, status: stable
    2. `<StatusBadge status="stable" />`
    3. Intro — 4 sizes (sm, md, lg, xl) × 6 states (default, hover, focused, filled, error, disabled)
    4. **Default** variant with `<ComponentPreview>`:
       - Render an inline-styled input element (use style={{}} not className for MDX compatibility)
       - Style: rounded-xl, border 1px #DEDEDE, bg white, padding, Ubuntu Sans font
       - Include placeholder text "Nombre completo"
    5. Code block showing the React+Tailwind implementation
    6. **With Label** variant — input with label above
    7. **Error State** variant — red border, error message below
    8. **Disabled State** variant — muted colors, cursor not-allowed
    9. **PropsTable** with props: size, state, label, placeholder, helperText, errorMessage, disabled, fullWidth, value, onChange
    10. **DoDont** guidelines:
        - Do: Use labels above inputs, use helper text for context
        - Don't: Use placeholder as label replacement

    All previews use inline styles in ComponentPreview. Code blocks use React+Tailwind.
    Content in Spanish.

    Key specs from DS:
    - Border: 1px solid #DEDEDE (default), #E74C3C (error)
    - Background: #FFFFFF
    - Border radius: 12px (--ds-radius-md)
    - Padding: 12px 16px (md size)
    - Font: Ubuntu Sans, 16px
    - Focus: border-color #0B0B0B
    - Disabled: bg #F6F6F6, text #999999
  </action>
  <verify>
    - `npm run build` succeeds
    - text-field.mdx has >80 lines with ComponentPreview
  </verify>
  <done>TextField page fully documented with live previews, code examples, PropsTable, and DoDont</done>
</task>

<task type="auto">
  <name>Task 2: Create Select and Checkbox component pages</name>
  <files>content/docs/components/select.mdx, content/docs/components/checkbox.mdx</files>
  <action>
    Create two new component documentation pages. Follow the same pattern as buttons.mdx.

    **select.mdx**:
    ```
    ---
    title: Select
    description: Selector desplegable con variantes y estados
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro
    2. **Default** variant with ComponentPreview — a styled select element
       - Style: same as TextField (rounded-xl, border #DEDEDE, bg white, padding)
       - Include sample options ("Seleccionar...", "Opcion 1", "Opcion 2")
    3. Code block with React+Tailwind
    4. **With Label** variant
    5. **Disabled** variant
    6. PropsTable: size, label, placeholder, options, value, onChange, disabled, fullWidth
    7. DoDont guidelines

    Key specs:
    - Same border/radius/padding as TextField
    - Custom dropdown arrow icon (chevron-down SVG or CSS)
    - Appearance: none to remove native styling

    **checkbox.mdx**:
    ```
    ---
    title: Checkbox
    description: Checkbox con label y estados checked, indeterminate, disabled
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro
    2. **Unchecked** variant with ComponentPreview — checkbox + label
       - Style: 20px × 20px, border 1.5px #DEDEDE, rounded-md (8px), bg white
    3. **Checked** variant — bg #0B0B0B, white checkmark SVG
    4. **Disabled** variant
    5. Code blocks for each variant
    6. PropsTable: checked, indeterminate, disabled, label, onChange, size
    7. DoDont guidelines

    Key specs:
    - Size: 20×20px (md), 16×16px (sm)
    - Checked: bg #0B0B0B, border #0B0B0B, white check icon
    - Border radius: 6px
    - Label: Ubuntu Sans 16px, gap 8px from checkbox

    All ComponentPreview content uses inline styles. Content in Spanish.
  </action>
  <verify>
    - `npm run build` succeeds
    - select.mdx and checkbox.mdx exist with >80 lines each
    - Both contain ComponentPreview
  </verify>
  <done>Select and Checkbox pages created with live previews, code examples, PropsTable, and DoDont</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] TextField placeholder replaced with full content
- [ ] Select and Checkbox pages created
- [ ] All three have ComponentPreview, code blocks, PropsTable, DoDont
</verification>

<success_criteria>

- All tasks completed
- Three form input components documented
- Build succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/08-component-docs-core/08-01-SUMMARY.md`
</output>
