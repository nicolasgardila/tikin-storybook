---
phase: 03-content-components
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - source.config.ts
  - lib/source.ts
  - app/docs/layout.tsx
  - content/docs/components/buttons.mdx
  - content/docs/components/text-field.mdx
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Sidebar items for component pages show a small status indicator (colored dot or badge) next to the page name"
    - "The status indicator in the sidebar reflects the frontmatter `status` field value (alpha/beta/stable)"
  artifacts:
    - path: "app/docs/layout.tsx"
      provides: "DocsLayout with sidebar status indicator integration"
    - path: "source.config.ts"
      provides: "Custom frontmatter schema with status field"
  key_links:
    - from: "content/docs/components/buttons.mdx"
      to: "app/docs/layout.tsx"
      via: "frontmatter status field → sidebar page tree decoration"
      pattern: "status"
---

<objective>
Close gap: Wire StatusBadge/status indicators into sidebar navigation items.

Purpose: CONT-06 requires status badges on both page headers AND sidebar items. Page headers work, sidebar is missing.
Output: Sidebar items show status indicators for components with a `status` frontmatter field.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-components/03-VERIFICATION.md
@.planning/phases/03-content-components/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add status frontmatter field and sidebar status indicators</name>
  <files>source.config.ts, lib/source.ts, app/docs/layout.tsx, content/docs/components/buttons.mdx, content/docs/components/text-field.mdx</files>
  <action>
    There are multiple approaches to show status in the sidebar. Choose the simplest that works with fumadocs-ui:

    **Approach A (preferred — fumadocs icon field):**

    1. Check if fumadocs-mdx supports a custom `icon` frontmatter field that renders in the sidebar. If so:
       - Add `status: stable` to buttons.mdx and `status: alpha` to text-field.mdx frontmatter
       - In `app/docs/layout.tsx`, use the `sidebar` prop to customize how sidebar items render. fumadocs-ui DocsLayout accepts `sidebar.components` or similar customization.

    2. Alternatively, transform the `source.pageTree` before passing it to DocsLayout:
       - Read each page's frontmatter to check for `status` field
       - Map the page tree to add a small colored dot icon next to items with a status

    **Approach B (simpler — CSS + data attribute):**

    1. Extend the fumadocs-mdx schema in `source.config.ts` to include a `status` field:
       ```typescript
       import { defineDocs } from 'fumadocs-mdx/config';
       import { z } from 'zod';

       export const docs = defineDocs({
         dir: 'content/docs',
         docs: {
           schema: {
             status: z.enum(['alpha', 'beta', 'stable']).optional(),
           },
         },
       });
       ```

    2. Create a utility that transforms the page tree to add status icons:
       ```typescript
       // In lib/source.ts or a new file
       function addStatusIcons(tree) {
         // Walk tree, for each page node, check if page has status
         // If so, add an icon element (small colored dot)
       }
       ```

    3. In `app/docs/layout.tsx`, pass the transformed tree:
       ```tsx
       <DocsLayout tree={transformedTree} ... />
       ```

    **Approach C (simplest — just use fumadocs icon in frontmatter):**

    fumadocs supports `icon` in MDX frontmatter that automatically renders in sidebar. Check if this works:
    1. Add to buttons.mdx frontmatter: `icon: CircleDot` or similar
    2. If fumadocs renders it in sidebar, use that mechanism with a status-colored icon

    **Choose whichever approach works. The key requirement is:**
    - Component pages show a visual status indicator in the sidebar
    - The indicator corresponds to the component's maturity (alpha=amber, beta=blue, stable=green)

    4. Add `status: stable` to `content/docs/components/buttons.mdx` frontmatter
    5. Add `status: alpha` to `content/docs/components/text-field.mdx` frontmatter

    IMPORTANT: Read the fumadocs-ui source/docs to understand what sidebar customization is available before implementing. The DocsLayout component may have props like `sidebar.components`, `sidebar.banner`, or page tree item transformation that enable this.
  </action>
  <verify>
    - `npm run build` succeeds
    - Sidebar shows status indicators next to Buttons and TextField items
    - Status indicator colors match: stable=green, alpha=amber
  </verify>
  <done>Sidebar items show status indicators reflecting frontmatter status field</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Sidebar shows status indicators for component pages
- [ ] Status matches frontmatter values (stable=green, alpha=amber)
- [ ] Other sidebar items (Foundations, Guides) are unaffected
</verification>

<success_criteria>

- Gap closed: StatusBadge/status indicators appear on sidebar items
- CONT-06 fully satisfied (both sidebar and page headers)
- Build succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/03-content-components/03-04-SUMMARY.md`
</output>
