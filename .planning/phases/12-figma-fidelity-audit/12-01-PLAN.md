---
phase: 12-figma-fidelity-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ds/ds-text-field.tsx
  - components/ds/ds-select.tsx
  - components/ds/ds-checkbox.tsx
  - content/docs/components/text-field.mdx
  - content/docs/components/select.mdx
  - content/docs/components/checkbox.mdx
  - content/docs/components/radio-button.mdx
  - content/docs/components/otp-input.mdx
autonomous: true
must_haves:
  truths:
    - "Each component's specs match Figma exactly (sizes, radii, colors, borders, spacing, fonts)"
    - "TextField DS component and MDX preview match Figma border-radius, border-width, border-color, padding, font-family, font-size, heights per state"
    - "Select DS component and MDX preview match Figma specs for dropdown trigger and chevron positioning"
    - "Checkbox DS component and MDX preview match Figma specs for box size, border-radius, check icon, label spacing"
    - "Radio Button MDX preview matches Figma specs for circle size, inner dot, label spacing"
    - "OTP Input MDX preview matches Figma specs for box dimensions, gap, border styles per state"
  artifacts:
    - path: "components/ds/ds-text-field.tsx"
      provides: "Figma-accurate TextField DS component with spec comments"
    - path: "components/ds/ds-select.tsx"
      provides: "Figma-accurate Select DS component with spec comments"
    - path: "components/ds/ds-checkbox.tsx"
      provides: "Figma-accurate Checkbox DS component with spec comments"
    - path: "content/docs/components/text-field.mdx"
      provides: "TextField MDX with Figma-accurate inline styles"
    - path: "content/docs/components/select.mdx"
      provides: "Select MDX with Figma-accurate inline styles"
    - path: "content/docs/components/checkbox.mdx"
      provides: "Checkbox MDX with Figma-accurate inline styles"
    - path: "content/docs/components/radio-button.mdx"
      provides: "Radio Button MDX with Figma-accurate inline styles"
    - path: "content/docs/components/otp-input.mdx"
      provides: "OTP Input MDX with Figma-accurate inline styles"
  key_links: []
---

<objective>
Audit and fix Form Input components (TextField, Select, Checkbox, Radio Button, OTP Input) to match Figma specs exactly.

Purpose: Extract exact CSS specs from HTML source files and Figma, then fix every discrepancy in DS components and static MDX previews — ensuring pixel-perfect fidelity for all form input components.
Output: All 5 form input components match Figma specs exactly in both DS playground components and static ComponentPreview inline styles.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@components/ds/ds-text-field.tsx
@components/ds/ds-select.tsx
@components/ds/ds-checkbox.tsx
@content/docs/components/text-field.mdx
@content/docs/components/select.mdx
@content/docs/components/checkbox.mdx
@content/docs/components/radio-button.mdx
@content/docs/components/otp-input.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Figma specs for all Form Input components</name>
  <files>components/ds/ds-text-field.tsx, components/ds/ds-select.tsx, components/ds/ds-checkbox.tsx</files>
  <action>
    Read the HTML source files that contain exact CSS specs from Figma for each component:

    - **Radio Button**: `/Users/nicolasgarcia/Tikin/checkbox-ds/radio.html`
    - **OTP Input**: `/Users/nicolasgarcia/Tikin/checkbox-ds/otp.html`

    For TextField, Select, and Checkbox — these do not have dedicated HTML files in checkbox-ds/. Use the Figma file via `get_design_context` MCP tool with file key `vGu558KBAfGjAhT498KWEF` to extract specs for these components. Search for nodes named "TextField", "Select", "Checkbox".

    For each component, extract a complete spec sheet covering:
    - Box dimensions (width, height per size variant)
    - Border-radius values
    - Border-width and border-color per state (default, hover, focus, error, disabled)
    - Background-color per state (default, hover, focus, error, disabled) for both light and dark themes
    - Padding (top, right, bottom, left)
    - Font-family, font-size, font-weight, line-height, letter-spacing
    - Text color per state for both light and dark themes
    - Placeholder color for both themes
    - Label font specs (family, size, weight, color, spacing from input)
    - Icon sizes and colors if applicable
    - Gap between elements
    - Shadow values if any
    - Transition/animation specs if specified

    Write the extracted specs as a structured comment block at the top of each DS component file:
    - `components/ds/ds-text-field.tsx` — TextField specs
    - `components/ds/ds-select.tsx` — Select specs
    - `components/ds/ds-checkbox.tsx` — Checkbox specs

    Format the spec comment like:
    ```
    /**
     * FIGMA SPECS — TextField
     * Source: checkbox-ds/[file].html + Figma file vGu558KBAfGjAhT498KWEF
     *
     * Sizes:
     *   sm: height 36px, font-size 14px, padding 8px 12px
     *   md: height 44px, font-size 14px, padding 12px 16px
     *   lg: height 52px, font-size 16px, padding 14px 16px
     *
     * Border:
     *   radius: 12px
     *   width: 1px
     *   color-default-light: #DEDEDE
     *   color-default-dark: #333333
     *   color-focus: var(--ds-brand)
     *   color-error: #E53E3E
     *
     * Background:
     *   light: #FFFFFF
     *   dark: #1E1E1E
     *   disabled-light: #F6F6F6
     *   disabled-dark: #1A1A1A
     *
     * Typography:
     *   font: Ubuntu Sans, 400
     *   placeholder-light: #9CA3AF
     *   placeholder-dark: #6B7280
     *   text-light: #0B0B0B
     *   text-dark: #FFFFFF
     */
    ```

    Also document Radio Button and OTP Input specs — since these have no DS component files, write the specs as a code comment block that will be used for reference in Task 2 when updating MDX files.
  </action>
  <verify>
    - Each DS component file has a Figma spec comment block at the top
    - All states (default, hover, focus, error, disabled) are documented
    - Both light and dark theme values are captured
    - Radio and OTP specs are documented for MDX reference
  </verify>
  <done>Complete spec sheets extracted for TextField, Select, Checkbox, Radio Button, and OTP Input</done>
</task>

<task type="auto">
  <name>Task 2: Fix all discrepancies in DS components and MDX previews</name>
  <files>components/ds/ds-text-field.tsx, components/ds/ds-select.tsx, components/ds/ds-checkbox.tsx, content/docs/components/text-field.mdx, content/docs/components/select.mdx, content/docs/components/checkbox.mdx, content/docs/components/radio-button.mdx, content/docs/components/otp-input.mdx</files>
  <action>
    Using the spec sheets from Task 1, systematically compare current implementations against Figma specs and fix every discrepancy.

    **For each DS component (ds-text-field.tsx, ds-select.tsx, ds-checkbox.tsx):**
    1. Read the current implementation
    2. Compare each CSS property against the Figma spec comment
    3. Fix: border-radius, border-width, border-color, background-color, padding, font-family, font-size, font-weight, height, color, placeholder color, disabled styles, focus styles, error styles
    4. Ensure --preview-* CSS variables are used correctly for theme adaptation
    5. Verify size variants (sm/md/lg) match Figma dimensions exactly

    **For each MDX file (text-field.mdx, select.mdx, checkbox.mdx, radio-button.mdx, otp-input.mdx):**
    1. Read all ComponentPreview sections with inline styles
    2. Compare each inline style value against the Figma spec
    3. Fix every discrepancy: borderRadius, borderWidth, borderColor, backgroundColor, padding, fontSize, fontFamily, fontWeight, color, height, gap, etc.
    4. Ensure both light-mode and dark-mode preview values use correct --preview-* variables
    5. Update anatomy tables if spec values differ from documented values

    **Common discrepancies to watch for:**
    - Border-radius values (e.g., 12px vs 8px vs 16px)
    - Border-width (0.5px vs 1px)
    - Padding values not matching Figma spacing tokens
    - Font-family mismatches (Ubuntu Sans vs Ubuntu Sans Mono)
    - Height not matching component height tokens
    - Color hex values not matching Figma palette
    - Missing state styles (focus ring, error border, disabled opacity)

    After fixing, run `npm run build` to verify no compilation errors.
  </action>
  <verify>
    - `npm run build` succeeds
    - DS component inline styles match Figma spec comments exactly
    - MDX ComponentPreview inline styles match Figma spec comments exactly
    - Anatomy tables reflect correct Figma values
    - All size variants render at correct Figma dimensions
  </verify>
  <done>All 5 form input components match Figma specs exactly in both DS components and MDX previews</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] TextField DS component + MDX preview match Figma specs
- [ ] Select DS component + MDX preview match Figma specs
- [ ] Checkbox DS component + MDX preview match Figma specs
- [ ] Radio Button MDX preview matches Figma specs
- [ ] OTP Input MDX preview matches Figma specs
- [ ] All spec comments are present at top of DS component files
</verification>

<success_criteria>
- All 5 form input components audited and fixed
- Every CSS property matches Figma specs exactly
- Both DS playground components and static MDX previews are corrected
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-figma-fidelity-audit/12-01-SUMMARY.md`
</output>
