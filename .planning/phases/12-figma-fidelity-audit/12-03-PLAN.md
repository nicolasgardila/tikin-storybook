---
phase: 12-figma-fidelity-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ds/ds-card.tsx
  - components/ds/ds-dialog.tsx
  - content/docs/components/card.mdx
  - content/docs/components/container.mdx
  - content/docs/components/dialog.mdx
  - content/docs/components/bottom-sheet.mdx
  - content/docs/components/side-panel.mdx
autonomous: true
must_haves:
  truths:
    - "Each component's specs match Figma exactly (sizes, radii, colors, borders, spacing, fonts)"
    - "Card DS component and MDX preview match Figma border-radius (24px), border-width (0.5px), border-color per variant/theme, background-color, padding"
    - "Dialog DS component and MDX preview match Figma overlay, container radius, padding, backdrop color, button layout"
    - "Container MDX preview matches Figma shell/panel nesting, padding, border-radius, background colors"
    - "Bottom Sheet MDX preview matches Figma handle dimensions, border-radius, height, backdrop"
    - "Side Panel MDX preview matches Figma width, header height, padding, border styles"
  artifacts:
    - path: "components/ds/ds-card.tsx"
      provides: "Figma-accurate Card DS component with spec comments"
    - path: "components/ds/ds-dialog.tsx"
      provides: "Figma-accurate Dialog DS component with spec comments"
    - path: "content/docs/components/card.mdx"
      provides: "Card MDX with Figma-accurate inline styles"
    - path: "content/docs/components/container.mdx"
      provides: "Container MDX with Figma-accurate inline styles"
    - path: "content/docs/components/dialog.mdx"
      provides: "Dialog MDX with Figma-accurate inline styles"
    - path: "content/docs/components/bottom-sheet.mdx"
      provides: "Bottom Sheet MDX with Figma-accurate inline styles"
    - path: "content/docs/components/side-panel.mdx"
      provides: "Side Panel MDX with Figma-accurate inline styles"
  key_links: []
---

<objective>
Audit and fix Layout components (Card, Container, Dialog, Bottom Sheet, Side Panel) to match Figma specs exactly.

Purpose: Extract exact CSS specs from HTML source files and Figma, then fix every discrepancy in DS components and static MDX previews — ensuring pixel-perfect fidelity for all layout/surface components.
Output: All 5 layout components match Figma specs exactly in both DS playground components and static ComponentPreview inline styles.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@components/ds/ds-card.tsx
@components/ds/ds-dialog.tsx
@content/docs/components/card.mdx
@content/docs/components/container.mdx
@content/docs/components/dialog.mdx
@content/docs/components/bottom-sheet.mdx
@content/docs/components/side-panel.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Figma specs for all Layout components</name>
  <files>components/ds/ds-card.tsx, components/ds/ds-dialog.tsx</files>
  <action>
    Read the HTML source files that contain exact CSS specs from Figma for each component:

    - **Card**: `/Users/nicolasgarcia/Tikin/checkbox-ds/card.html`
    - **Dialog/Modal**: `/Users/nicolasgarcia/Tikin/checkbox-ds/modal.html`
    - **Bottom Sheet**: `/Users/nicolasgarcia/Tikin/checkbox-ds/bottomsheet.html`
    - **Side Panel**: `/Users/nicolasgarcia/Tikin/checkbox-ds/sidepanel.html`

    For Container — this does not have a dedicated HTML file in checkbox-ds/. Use the Figma file via `get_design_context` MCP tool with file key `vGu558KBAfGjAhT498KWEF` to extract specs. Search for nodes named "Container".

    For each component, extract a complete spec sheet covering:
    - Box dimensions (width, height, max-width, max-height)
    - Border-radius values (container + inner elements)
    - Border-width and border-color per variant for both light and dark themes
    - Background-color per variant for both light and dark themes
    - Padding (body, header, footer sections)
    - Font specs for title, subtitle, body text (family, size, weight, color)
    - Shadow values (especially for elevated card, dialog overlay)
    - Gap between sections/elements
    - For Card: all variants (default/warm, neutral, filled, elevated, outline)
    - For Dialog: backdrop color/opacity, container dimensions, button area layout, close icon size
    - For Bottom Sheet: handle bar dimensions, handle bar color, top border-radius, max-height
    - For Side Panel: width, header height, close button, border-left/right per side

    Write the extracted specs as a structured comment block at the top of each DS component file:
    - `components/ds/ds-card.tsx` — Card specs (all variants)
    - `components/ds/ds-dialog.tsx` — Dialog specs

    Also document Container, Bottom Sheet, and Side Panel specs for MDX reference in Task 2.
  </action>
  <verify>
    - Each DS component file has a Figma spec comment block at the top
    - All variants documented with both light and dark theme values
    - Container, Bottom Sheet, Side Panel specs documented for MDX reference
  </verify>
  <done>Complete spec sheets extracted for Card, Container, Dialog, Bottom Sheet, and Side Panel</done>
</task>

<task type="auto">
  <name>Task 2: Fix all discrepancies in DS components and MDX previews</name>
  <files>components/ds/ds-card.tsx, components/ds/ds-dialog.tsx, content/docs/components/card.mdx, content/docs/components/container.mdx, content/docs/components/dialog.mdx, content/docs/components/bottom-sheet.mdx, content/docs/components/side-panel.mdx</files>
  <action>
    Using the spec sheets from Task 1, systematically compare current implementations against Figma specs and fix every discrepancy.

    **For each DS component (ds-card.tsx, ds-dialog.tsx):**
    1. Read the current implementation
    2. Compare each CSS property against the Figma spec comment
    3. Fix: border-radius, border-width, border-color, background-color, padding, font-family, font-size, font-weight, shadow, gap
    4. For ds-card.tsx: verify all variants (default, neutral, filled, elevated, outline) match card.html specs exactly — especially border-radius 24px, border 0.5px solid, correct border-color per variant/theme
    5. For ds-dialog.tsx: verify backdrop, container radius, padding, button layout, title font specs
    6. Ensure --preview-* CSS variables are used correctly for theme adaptation

    **For each MDX file (card.mdx, container.mdx, dialog.mdx, bottom-sheet.mdx, side-panel.mdx):**
    1. Read all ComponentPreview sections with inline styles
    2. Compare each inline style value against the Figma spec
    3. Fix every discrepancy in inline styles
    4. Update anatomy tables if spec values differ from documented values
    5. Pay special attention to:
       - Card border-radius (should be 24px per card.html)
       - Card border-width (should be 0.5px per card.html)
       - Card variant-specific border colors (warm: #E8E4DD light / #2A2826 dark, neutral: #DEDEDE light / #333333 dark)
       - Dialog backdrop opacity
       - Bottom Sheet handle bar exact dimensions and color
       - Side Panel width and border styles

    After fixing, run `npm run build` to verify no compilation errors.
  </action>
  <verify>
    - `npm run build` succeeds
    - DS component inline styles match Figma spec comments exactly
    - MDX ComponentPreview inline styles match Figma spec comments exactly
    - Card variants all use correct border-radius (24px) and border-width (0.5px)
    - Anatomy tables reflect correct Figma values
  </verify>
  <done>All 5 layout components match Figma specs exactly in both DS components and MDX previews</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Card DS component + MDX preview match Figma specs (all 5 variants)
- [ ] Dialog DS component + MDX preview match Figma specs
- [ ] Container MDX preview matches Figma specs
- [ ] Bottom Sheet MDX preview matches Figma specs
- [ ] Side Panel MDX preview matches Figma specs
- [ ] All spec comments are present at top of DS component files
</verification>

<success_criteria>
- All 5 layout components audited and fixed
- Every CSS property matches Figma specs exactly
- Both DS playground components and static MDX previews are corrected
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-figma-fidelity-audit/12-03-SUMMARY.md`
</output>
