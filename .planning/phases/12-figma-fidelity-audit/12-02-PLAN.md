---
phase: 12-figma-fidelity-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ds/ds-button.tsx
  - components/ds/ds-toggle.tsx
  - components/ds/ds-tabs.tsx
  - content/docs/components/buttons.mdx
  - content/docs/components/toggle.mdx
  - content/docs/components/tabs.mdx
  - content/docs/components/segmented-control.mdx
  - content/docs/components/search-bar.mdx
autonomous: true
must_haves:
  truths:
    - "Each component's specs match Figma exactly (sizes, radii, colors, borders, spacing, fonts)"
    - "Button DS component and MDX preview match Figma border-radius, padding, font-family, font-size, font-weight, letter-spacing, text-transform, colors per variant"
    - "Toggle DS component and MDX preview match Figma track/thumb dimensions, colors, transition, border-radius"
    - "Tabs DS component and MDX preview match Figma tab heights, indicator styles, font specs, active/inactive colors"
    - "Segmented Control MDX preview matches Figma container size, segment padding, border-radius, active indicator"
    - "Search Bar MDX preview matches Figma input height, icon size, padding, border-radius, placeholder color"
  artifacts:
    - path: "components/ds/ds-button.tsx"
      provides: "Figma-accurate Button DS component with spec comments"
    - path: "components/ds/ds-toggle.tsx"
      provides: "Figma-accurate Toggle DS component with spec comments"
    - path: "components/ds/ds-tabs.tsx"
      provides: "Figma-accurate Tabs DS component with spec comments"
    - path: "content/docs/components/buttons.mdx"
      provides: "Buttons MDX with Figma-accurate inline styles"
    - path: "content/docs/components/toggle.mdx"
      provides: "Toggle MDX with Figma-accurate inline styles"
    - path: "content/docs/components/tabs.mdx"
      provides: "Tabs MDX with Figma-accurate inline styles"
    - path: "content/docs/components/segmented-control.mdx"
      provides: "Segmented Control MDX with Figma-accurate inline styles"
    - path: "content/docs/components/search-bar.mdx"
      provides: "Search Bar MDX with Figma-accurate inline styles"
  key_links: []
---

<objective>
Audit and fix Interactive Control components (Buttons, Toggle, Tabs, Segmented Control, Search Bar) to match Figma specs exactly.

Purpose: Extract exact CSS specs from HTML source files and Figma, then fix every discrepancy in DS components and static MDX previews — ensuring pixel-perfect fidelity for all interactive control components.
Output: All 5 interactive control components match Figma specs exactly in both DS playground components and static ComponentPreview inline styles.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@components/ds/ds-button.tsx
@components/ds/ds-toggle.tsx
@components/ds/ds-tabs.tsx
@content/docs/components/buttons.mdx
@content/docs/components/toggle.mdx
@content/docs/components/tabs.mdx
@content/docs/components/segmented-control.mdx
@content/docs/components/search-bar.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Figma specs for all Interactive Control components</name>
  <files>components/ds/ds-button.tsx, components/ds/ds-toggle.tsx, components/ds/ds-tabs.tsx</files>
  <action>
    Read the HTML source files that contain exact CSS specs from Figma for each component:

    - **Tabs**: `/Users/nicolasgarcia/Tikin/checkbox-ds/tabs.html`
    - **Segmented Control**: `/Users/nicolasgarcia/Tikin/checkbox-ds/segmented.html`
    - **Search Bar**: `/Users/nicolasgarcia/Tikin/checkbox-ds/search.html`

    For Buttons and Toggle — these do not have dedicated HTML files in checkbox-ds/. Use the Figma file via `get_design_context` MCP tool with file key `vGu558KBAfGjAhT498KWEF` to extract specs for these components. Search for nodes named "Button", "Toggle".

    For each component, extract a complete spec sheet covering:
    - Box dimensions (width, height per size variant)
    - Border-radius values
    - Border-width and border-color per state and variant
    - Background-color per state and variant for both light and dark themes
    - Padding (top, right, bottom, left)
    - Font-family, font-size, font-weight, line-height, letter-spacing, text-transform
    - Text color per state/variant for both light and dark themes
    - Icon sizes and colors if applicable
    - Gap between elements
    - Shadow values if any
    - Transition/animation specs
    - For Toggle: track dimensions, thumb dimensions, thumb offset, active/inactive colors
    - For Tabs: indicator height, indicator border-radius, tab padding, active/inactive text colors
    - For Segmented Control: container height, segment padding, active segment background, border-radius
    - For Search Bar: input height, search icon size, clear icon size, padding, placeholder styles

    Write the extracted specs as a structured comment block at the top of each DS component file:
    - `components/ds/ds-button.tsx` — Button specs (all 7 variants)
    - `components/ds/ds-toggle.tsx` — Toggle specs
    - `components/ds/ds-tabs.tsx` — Tabs specs (contained + underline variants)

    Also document Segmented Control and Search Bar specs for MDX reference in Task 2.
  </action>
  <verify>
    - Each DS component file has a Figma spec comment block at the top
    - All states and variants are documented
    - Both light and dark theme values are captured
    - Segmented Control and Search Bar specs documented for MDX reference
  </verify>
  <done>Complete spec sheets extracted for Buttons, Toggle, Tabs, Segmented Control, and Search Bar</done>
</task>

<task type="auto">
  <name>Task 2: Fix all discrepancies in DS components and MDX previews</name>
  <files>components/ds/ds-button.tsx, components/ds/ds-toggle.tsx, components/ds/ds-tabs.tsx, content/docs/components/buttons.mdx, content/docs/components/toggle.mdx, content/docs/components/tabs.mdx, content/docs/components/segmented-control.mdx, content/docs/components/search-bar.mdx</files>
  <action>
    Using the spec sheets from Task 1, systematically compare current implementations against Figma specs and fix every discrepancy.

    **For each DS component (ds-button.tsx, ds-toggle.tsx, ds-tabs.tsx):**
    1. Read the current implementation
    2. Compare each CSS property against the Figma spec comment
    3. Fix: border-radius, border-width, border-color, background-color, padding, font-family, font-size, font-weight, height, color, letter-spacing, text-transform, shadow, transition
    4. For ds-button.tsx: verify all 7 variants (primary, secondary, ghost, link, icon-primary, icon-secondary, icon-muted) and all sizes (sm, md, lg, xl)
    5. For ds-toggle.tsx: verify track/thumb dimensions (44x24px track, 20x20px thumb per existing decision), colors, transition timing
    6. For ds-tabs.tsx: verify both contained and underline variants, indicator styles, active/inactive colors
    7. Ensure --preview-* CSS variables are used correctly for theme adaptation

    **For each MDX file (buttons.mdx, toggle.mdx, tabs.mdx, segmented-control.mdx, search-bar.mdx):**
    1. Read all ComponentPreview sections with inline styles
    2. Compare each inline style value against the Figma spec
    3. Fix every discrepancy in inline styles
    4. Update anatomy tables if spec values differ from documented values

    **Common discrepancies to watch for:**
    - Button padding not matching Figma per size variant
    - Toggle track/thumb colors not matching Figma active/inactive states
    - Tabs indicator position/size not matching Figma
    - Segmented Control active segment styling
    - Search bar icon positioning and sizing
    - Font-family mismatches between variants

    After fixing, run `npm run build` to verify no compilation errors.
  </action>
  <verify>
    - `npm run build` succeeds
    - DS component inline styles match Figma spec comments exactly
    - MDX ComponentPreview inline styles match Figma spec comments exactly
    - Anatomy tables reflect correct Figma values
    - All size/variant combinations render at correct Figma dimensions
  </verify>
  <done>All 5 interactive control components match Figma specs exactly in both DS components and MDX previews</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Button DS component + MDX preview match Figma specs (all 7 variants)
- [ ] Toggle DS component + MDX preview match Figma specs
- [ ] Tabs DS component + MDX preview match Figma specs (contained + underline)
- [ ] Segmented Control MDX preview matches Figma specs
- [ ] Search Bar MDX preview matches Figma specs
- [ ] All spec comments are present at top of DS component files
</verification>

<success_criteria>
- All 5 interactive control components audited and fixed
- Every CSS property matches Figma specs exactly
- Both DS playground components and static MDX previews are corrected
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-figma-fidelity-audit/12-02-SUMMARY.md`
</output>
