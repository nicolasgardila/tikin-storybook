---
phase: 12-figma-fidelity-audit
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ds/ds-toast.tsx
  - components/ds/ds-alert.tsx
  - content/docs/components/toast.mdx
  - content/docs/components/alert.mdx
  - content/docs/components/snackbar.mdx
  - content/docs/components/tooltip.mdx
  - content/docs/components/chip.mdx
autonomous: true
must_haves:
  truths:
    - "Each component's specs match Figma exactly (sizes, radii, colors, borders, spacing, fonts)"
    - "Toast DS component and MDX preview match Figma background, border-radius, padding, font specs, icon size"
    - "Alert DS component and MDX preview match Figma variant colors, border-radius, padding, icon positioning"
    - "Snackbar MDX preview matches Figma dimensions, background, border-radius, action button styles"
    - "Tooltip MDX preview matches Figma arrow size, padding, border-radius, background, font specs"
    - "Chip MDX preview matches Figma height, padding, border-radius, border, font specs, close icon size"
  artifacts:
    - path: "components/ds/ds-toast.tsx"
      provides: "Figma-accurate Toast DS component with spec comments"
    - path: "components/ds/ds-alert.tsx"
      provides: "Figma-accurate Alert DS component with spec comments"
    - path: "content/docs/components/toast.mdx"
      provides: "Toast MDX with Figma-accurate inline styles"
    - path: "content/docs/components/alert.mdx"
      provides: "Alert MDX with Figma-accurate inline styles"
    - path: "content/docs/components/snackbar.mdx"
      provides: "Snackbar MDX with Figma-accurate inline styles"
    - path: "content/docs/components/tooltip.mdx"
      provides: "Tooltip MDX with Figma-accurate inline styles"
    - path: "content/docs/components/chip.mdx"
      provides: "Chip MDX with Figma-accurate inline styles"
  key_links: []
---

<objective>
Audit and fix Feedback components (Toast, Alert, Snackbar, Tooltip, Chip) to match Figma specs exactly.

Purpose: Extract exact CSS specs from HTML source files and Figma, then fix every discrepancy in DS components and static MDX previews — ensuring pixel-perfect fidelity for all feedback/notification components.
Output: All 5 feedback components match Figma specs exactly in both DS playground components and static ComponentPreview inline styles.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@components/ds/ds-toast.tsx
@components/ds/ds-alert.tsx
@content/docs/components/toast.mdx
@content/docs/components/alert.mdx
@content/docs/components/snackbar.mdx
@content/docs/components/tooltip.mdx
@content/docs/components/chip.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Figma specs for all Feedback components</name>
  <files>components/ds/ds-toast.tsx, components/ds/ds-alert.tsx</files>
  <action>
    Read the HTML source files that contain exact CSS specs from Figma for each component:

    - **Alert**: `/Users/nicolasgarcia/Tikin/checkbox-ds/alert.html`
    - **Snackbar**: `/Users/nicolasgarcia/Tikin/checkbox-ds/snackbar.html`
    - **Tooltip**: `/Users/nicolasgarcia/Tikin/checkbox-ds/tooltip.html`
    - **Chip**: `/Users/nicolasgarcia/Tikin/checkbox-ds/chip.html`

    For Toast — this does not have a dedicated HTML file in checkbox-ds/. Use the Figma file via `get_design_context` MCP tool with file key `vGu558KBAfGjAhT498KWEF` to extract specs. Search for nodes named "Toast", "Notification".

    For each component, extract a complete spec sheet covering:
    - Box dimensions (width, min-width, max-width, height)
    - Border-radius values
    - Border-width and border-color per variant for both light and dark themes
    - Background-color per variant for both light and dark themes
    - Padding (top, right, bottom, left)
    - Font specs for title, description, action text (family, size, weight, color)
    - Icon sizes, icon colors per variant
    - Gap between elements (icon-to-text, title-to-description, text-to-action)
    - Shadow values
    - For Alert: variant-specific background tints (info, success, warning, error), icon colors, border styles
    - For Snackbar: action button styles, dismiss icon, position hints
    - For Tooltip: arrow dimensions (6px triangle per existing decision), arrow color, max-width, text alignment
    - For Chip: height, horizontal padding, border-radius, close/remove icon size, selected state

    Write the extracted specs as a structured comment block at the top of each DS component file:
    - `components/ds/ds-toast.tsx` — Toast specs
    - `components/ds/ds-alert.tsx` — Alert specs (all variants)

    Also document Snackbar, Tooltip, and Chip specs for MDX reference in Task 2.
  </action>
  <verify>
    - Each DS component file has a Figma spec comment block at the top
    - All variants documented with both light and dark theme values
    - Snackbar, Tooltip, Chip specs documented for MDX reference
  </verify>
  <done>Complete spec sheets extracted for Toast, Alert, Snackbar, Tooltip, and Chip</done>
</task>

<task type="auto">
  <name>Task 2: Fix all discrepancies in DS components and MDX previews</name>
  <files>components/ds/ds-toast.tsx, components/ds/ds-alert.tsx, content/docs/components/toast.mdx, content/docs/components/alert.mdx, content/docs/components/snackbar.mdx, content/docs/components/tooltip.mdx, content/docs/components/chip.mdx</files>
  <action>
    Using the spec sheets from Task 1, systematically compare current implementations against Figma specs and fix every discrepancy.

    **For each DS component (ds-toast.tsx, ds-alert.tsx):**
    1. Read the current implementation
    2. Compare each CSS property against the Figma spec comment
    3. Fix: border-radius, background-color, padding, font-family, font-size, font-weight, color, shadow, gap, icon sizes
    4. For ds-toast.tsx: verify dark bg (#0B0B0B per existing decision), border-radius, padding, text colors, icon size
    5. For ds-alert.tsx: verify all variant background tints, icon colors, border styles, padding, font specs
    6. Ensure --preview-* CSS variables are used correctly for theme adaptation

    **For each MDX file (toast.mdx, alert.mdx, snackbar.mdx, tooltip.mdx, chip.mdx):**
    1. Read all ComponentPreview sections with inline styles
    2. Compare each inline style value against the Figma spec
    3. Fix every discrepancy in inline styles
    4. Update anatomy tables if spec values differ from documented values
    5. Pay special attention to:
       - Alert variant-specific background tint hex values
       - Snackbar dimensions and action button styles
       - Tooltip arrow CSS border triangle technique (6px per existing decision)
       - Chip height, padding, border-radius, close icon positioning

    After fixing, run `npm run build` to verify no compilation errors.
  </action>
  <verify>
    - `npm run build` succeeds
    - DS component inline styles match Figma spec comments exactly
    - MDX ComponentPreview inline styles match Figma spec comments exactly
    - Alert variant colors match Figma exactly
    - Anatomy tables reflect correct Figma values
  </verify>
  <done>All 5 feedback components match Figma specs exactly in both DS components and MDX previews</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Toast DS component + MDX preview match Figma specs
- [ ] Alert DS component + MDX preview match Figma specs (all variants)
- [ ] Snackbar MDX preview matches Figma specs
- [ ] Tooltip MDX preview matches Figma specs (including arrow)
- [ ] Chip MDX preview matches Figma specs
- [ ] All spec comments are present at top of DS component files
</verification>

<success_criteria>
- All 5 feedback components audited and fixed
- Every CSS property matches Figma specs exactly
- Both DS playground components and static MDX previews are corrected
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-figma-fidelity-audit/12-04-SUMMARY.md`
</output>
