---
phase: 05-preview-system
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - content/docs/components/buttons.mdx
autonomous: true
must_haves:
  truths:
    - "Buttons page shows live rendered React buttons in the ComponentPreview area"
    - "Desktop/Mobile toggle changes the preview width on the Buttons page"
    - "Light/Dark toggle changes the preview background on the Buttons page, independently of docs site theme"
    - "Multiple ComponentPreview instances on one page work independently"
  artifacts:
    - path: "content/docs/components/buttons.mdx"
      provides: "Buttons page with live ComponentPreview demos"
      contains: "ComponentPreview"
  key_links:
    - from: "content/docs/components/buttons.mdx"
      to: "components/docs/component-preview.tsx"
      via: "ComponentPreview wrapping live button demos"
      pattern: "ComponentPreview"
---

<objective>
Integrate ComponentPreview into the Buttons page with live demo components.

Purpose: Validate the preview system works end-to-end on a real component page before Phases 8-9 use it for 30+ components.
Output: Buttons page with live interactive previews showing viewport and theme toggling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-preview-system/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add live ComponentPreview demos to Buttons page</name>
  <files>content/docs/components/buttons.mdx</files>
  <action>
    Update `content/docs/components/buttons.mdx` to include ComponentPreview with live button demos. Add previews BEFORE each code block so the pattern is: preview → code → explanation.

    For each button variant section, add a ComponentPreview wrapping the actual rendered button:

    ```mdx
    ### Primary Button

    El botón principal para acciones primarias. Fondo oscuro con texto claro.

    <ComponentPreview>
      <button className="rounded-xl bg-[#0B0B0B] px-6 py-3 text-sm font-medium uppercase tracking-wider text-white font-[var(--font-ubuntu-sans-mono)]">
        Continuar
      </button>
    </ComponentPreview>

    ```tsx title="primary-button.tsx"
    // ... existing code block ...
    ```

    ### Secondary Button

    <ComponentPreview>
      <button className="rounded-xl border border-[#DEDEDE] bg-transparent px-6 py-3 text-sm font-medium uppercase tracking-wider text-[#0B0B0B] font-[var(--font-ubuntu-sans-mono)]">
        Cancelar
      </button>
    </ComponentPreview>

    ```tsx title="secondary-button.tsx"
    // ... existing code block ...
    ```

    ### Ghost Button

    <ComponentPreview>
      <button className="rounded-xl px-6 py-3 text-sm font-medium text-[#0B0B0B] hover:bg-[#F6F6F6] font-[var(--font-ubuntu-sans-mono)]">
        Ver más
      </button>
    </ComponentPreview>

    ```tsx title="ghost-button.tsx"
    // ... existing code block ...
    ```

    IMPORTANT:
    - The buttons inside ComponentPreview are LIVE rendered HTML — they're actual buttons the user can see and interact with
    - Use hardcoded hex values (not CSS variables) for button colors so they work correctly in both light and dark preview themes
    - For dark preview theme, the Secondary and Ghost buttons need to handle inverted text. The ComponentPreview sets `color` on the container, so text-[#0B0B0B] may not be visible. Use `text-inherit` or conditional classes if needed, OR accept that some buttons are designed for light backgrounds only.
    - Keep the existing PropsTable and DoDont sections unchanged
    - Each ComponentPreview is independent — toggling viewport/theme on one doesn't affect others
  </action>
  <verify>
    - `npm run build` succeeds
    - Navigate to /docs/components/buttons — previews render with live buttons
    - Desktop/Mobile toggle changes preview width
    - Light/Dark toggle changes preview background
    - Code blocks still render below each preview
    - PropsTable and DoDont sections unchanged
  </verify>
  <done>Buttons page has live ComponentPreview demos with viewport and theme toggles for each variant</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Buttons page shows live button previews
- [ ] Each preview has working desktop/mobile toggle
- [ ] Each preview has working light/dark toggle
- [ ] Preview theme is independent of docs site theme
- [ ] Code blocks and other sections still render correctly
</verification>

<success_criteria>

- All tasks completed
- Live previews work on Buttons page
- Viewport and theme toggles work independently per preview
- Build succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/05-preview-system/05-02-SUMMARY.md`
</output>
