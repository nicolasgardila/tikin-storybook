---
phase: 06-foundations-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - content/docs/foundations/radius.mdx
  - content/docs/foundations/shadows.mdx
  - content/docs/foundations/borders.mdx
  - content/docs/foundations/heights.mdx
  - app/global.css
autonomous: true
must_haves:
  truths:
    - "Radius page shows all 10 radius tokens with values and usage examples"
    - "Shadows page shows 5 shadow levels with visual descriptions"
    - "Border widths page shows 4 border tokens"
    - "Component heights page shows standard height tokens"
    - "Missing DS tokens (shadows, border widths, heights) are added to global.css"
  artifacts:
    - path: "content/docs/foundations/radius.mdx"
      provides: "Radius tokens documentation"
      min_lines: 30
    - path: "content/docs/foundations/shadows.mdx"
      provides: "Shadow levels documentation"
      min_lines: 30
    - path: "content/docs/foundations/borders.mdx"
      provides: "Border widths documentation"
      min_lines: 20
    - path: "content/docs/foundations/heights.mdx"
      provides: "Component heights documentation"
      min_lines: 20
    - path: "app/global.css"
      provides: "DS shadow, border-width, and height CSS custom properties"
      contains: "--ds-shadow"
  key_links:
    - from: "content/docs/foundations/shadows.mdx"
      to: "app/global.css"
      via: "Shadow tokens reference --ds-shadow-* variables"
      pattern: "--ds-shadow"
---

<objective>
Create four new foundations pages (Radius, Shadows, Borders, Heights) and add missing DS tokens to global.css.

Purpose: Complete the token reference for visual properties beyond color/type/spacing.
Output: Four new MDX documentation pages and updated CSS custom properties.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@app/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing DS tokens to global.css</name>
  <files>app/global.css</files>
  <action>
    Add shadow, border-width, and component height tokens to the DS tokens section in global.css `:root`.
    Place them AFTER the existing `--ds-radius-*` block, BEFORE the `.dark` block.

    **Shadows** (5 levels):
    ```css
    /* DS Shadows */
    --ds-shadow-xs: 0 1px 2px rgba(0, 0, 0, 0.05);
    --ds-shadow-sm: 0 2px 4px rgba(0, 0, 0, 0.06);
    --ds-shadow-md: 0 4px 8px rgba(0, 0, 0, 0.08);
    --ds-shadow-lg: 0 8px 16px rgba(0, 0, 0, 0.1);
    --ds-shadow-xl: 0 16px 32px rgba(0, 0, 0, 0.12);
    ```

    **Border Widths** (4 tokens):
    ```css
    /* DS Border Widths */
    --ds-border-thin: 1px;
    --ds-border-regular: 1.5px;
    --ds-border-medium: 2px;
    --ds-border-thick: 3px;
    ```

    **Component Heights** (standard heights):
    ```css
    /* DS Component Heights */
    --ds-height-xs: 28px;
    --ds-height-sm: 32px;
    --ds-height-md: 40px;
    --ds-height-lg: 48px;
    --ds-height-xl: 56px;
    ```

    IMPORTANT: Keep the existing comment structure. Add new sections with the same `/* DS ... */` comment pattern.
  </action>
  <verify>
    - `npm run build` succeeds
    - global.css contains --ds-shadow-xs through --ds-shadow-xl
    - global.css contains --ds-border-thin through --ds-border-thick
    - global.css contains --ds-height-xs through --ds-height-xl
  </verify>
  <done>All missing DS tokens added to global.css</done>
</task>

<task type="auto">
  <name>Task 2: Create Radius, Shadows, Borders, and Heights MDX pages</name>
  <files>content/docs/foundations/radius.mdx, content/docs/foundations/shadows.mdx, content/docs/foundations/borders.mdx, content/docs/foundations/heights.mdx</files>
  <action>
    Create four new MDX files in content/docs/foundations/. Content in Spanish.

    **radius.mdx**:
    ```
    ---
    title: Radio de Bordes
    description: 10 tokens de radio para esquinas redondeadas
    ---
    ```
    Content:
    1. Intro — explain the radius system and corner smoothing (60%)
    2. Token table with ALL radius tokens from global.css:

       | Token | CSS Variable | Value | Uso |
       | xs | --ds-radius-xs | 4px | Badges, chips pequenos |
       | sm | --ds-radius-sm | 8px | Inputs, botones pequenos |
       | md | --ds-radius-md | 12px | Tarjetas, containers internos |
       | lg | --ds-radius-lg | 16px | Modales, sheets |
       | xl | --ds-radius-xl | 20px | Paneles interiores (double-layer) |
       | 2xl | --ds-radius-2xl | 24px | Containers exteriores (double-layer) |
       | 3xl | --ds-radius-3xl | 28px | Grandes paneles |
       | 4xl | --ds-radius-4xl | 32px | Bottom sheets |
       | full | --ds-radius-full | 9999px | Avatares, circulos |
       | pill | --ds-radius-pill | 44px | Status chips, tags |

       Note: Check global.css — it currently only has sm, md, lg, xl, 2xl, pill. Add the missing radius tokens to global.css as part of this task (xs=4px, 3xl=28px, 4xl=32px, full=9999px).

    3. **Patron Double-Layer** — explain how outer shell uses r24 and inner panel uses r20
    4. Code example showing radius usage

    **shadows.mdx**:
    ```
    ---
    title: Sombras
    description: 5 niveles de sombra para elevacion visual
    ---
    ```
    Content:
    1. Intro — shadow system for visual elevation
    2. Token table — 5 levels with CSS variable, value, and usage context
    3. Usage guidelines — when to use each level
    4. Code example

    **borders.mdx**:
    ```
    ---
    title: Bordes
    description: 4 tokens de ancho de borde
    ---
    ```
    Content:
    1. Intro — border widths for different UI contexts
    2. Token table — 4 widths with usage
    3. Border colors reference (link back to Colors page warm/neutral borders)
    4. Code example

    **heights.mdx**:
    ```
    ---
    title: Alturas de Componentes
    description: Tokens de altura estandar para componentes interactivos
    ---
    ```
    Content:
    1. Intro — consistent heights for interactive components
    2. Token table — 5 heights with which components use them
       - xs (28px): Small chips, compact badges
       - sm (32px): Small buttons, compact inputs
       - md (40px): Default buttons, inputs, selects
       - lg (48px): Large buttons, search bars
       - xl (56px): Extra large inputs, prominent CTAs
    3. Code example

    IMPORTANT: Also add missing radius tokens to global.css `:root` section — add `--ds-radius-xs: 4px;`, `--ds-radius-3xl: 28px;`, `--ds-radius-4xl: 32px;`, and `--ds-radius-full: 9999px;` after the existing radius tokens.
  </action>
  <verify>
    - `npm run build` succeeds
    - All 4 new MDX files exist with substantial content
    - global.css has all 10 radius tokens
  </verify>
  <done>Four new token pages created with full documentation, all radius tokens in CSS</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] global.css has shadow, border-width, height, and complete radius tokens
- [ ] radius.mdx, shadows.mdx, borders.mdx, heights.mdx all exist with content
- [ ] No TypeScript or MDX errors
</verification>

<success_criteria>

- All tasks completed
- New CSS tokens added to global.css
- Four new MDX pages created with full content
- Build succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/06-foundations-content/06-02-SUMMARY.md`
</output>
