---
phase: 06-foundations-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/docs/foundations/colors.mdx
  - content/docs/foundations/typography.mdx
  - content/docs/foundations/spacing.mdx
autonomous: true
must_haves:
  truths:
    - "Colors page shows semantic tokens with warm/neutral dual-tone mapping and hex values"
    - "Typography page displays Funnel Display, Ubuntu Sans, Ubuntu Sans Mono with roles, sizes, and weights"
    - "Spacing page shows all 19 tokens from 0 to 64px with visual representations"
  artifacts:
    - path: "content/docs/foundations/colors.mdx"
      provides: "Full colors documentation with warm/neutral tones, semantic mapping, hex values"
      min_lines: 60
    - path: "content/docs/foundations/typography.mdx"
      provides: "Full typography documentation with all 3 fonts, roles, sizes, weights"
      min_lines: 60
    - path: "content/docs/foundations/spacing.mdx"
      provides: "Full spacing documentation with all 19 tokens and visual scale"
      min_lines: 40
  key_links: []
---

<objective>
Populate the three existing foundations placeholder pages (Colors, Typography, Spacing) with comprehensive token documentation.

Purpose: Replace placeholder content with full production token values so the team has a definitive reference.
Output: Three complete MDX documentation pages with all token values, usage guidance, and visual token representations using code blocks and tables.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/global.css
@content/docs/foundations/colors.mdx
@content/docs/foundations/typography.mdx
@content/docs/foundations/spacing.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate Colors page with full token documentation</name>
  <files>content/docs/foundations/colors.mdx</files>
  <action>
    Replace the placeholder content in colors.mdx with comprehensive color documentation.

    Content structure:
    1. **Sistema de Color** intro — explain dual-tone system (warm for panels/cards/wallet, neutral for dialogs/inputs)
    2. **Warm Tone** section — table with token name, CSS variable, hex value, and usage:
       - `--ds-warm-bg` (#FAF8F4) — page backgrounds, wallet views
       - `--ds-warm-surface` (#F1EFEB) — card surfaces, side panel backgrounds
       - `--ds-warm-border` (#E8E4DD) — organic container borders, card dividers
    3. **Neutral Tone** section — same format:
       - `--ds-neutral-bg` (#F6F6F6) — dialog backgrounds, tab containers, outer shells
       - `--ds-neutral-surface` (#FFFFFF) — inner panels, input backgrounds
       - `--ds-neutral-border` (#DEDEDE) — functional UI borders, input borders
    4. **Brand** section:
       - `--ds-brand` (#0B0B0B light / #FFFFFF dark) — primary brand color
       - `--ds-brand-on` (#FFFFFF light / #0B0B0B dark) — text on brand
    5. **Texto y Estado** section — text/icon colors used across components:
       - Primary text: #0B0B0B
       - Secondary text: #666666
       - Muted/disabled: #999999
       - Success: #2ECC71 / Error: #E74C3C / Warning: #F39C12
    6. **Dark Mode** note — explain that DS tones stay constant, only brand inverts
    7. **Uso en codigo** — code example showing CSS variable usage in Tailwind:
       ```tsx
       <div className="bg-[var(--ds-warm-bg)] border border-[var(--ds-warm-border)]">
         <span className="text-[var(--ds-brand)]">Texto principal</span>
       </div>
       ```

    Use tables for token listings. Include hex values in all entries.
    Content in Spanish to match existing pages.
  </action>
  <verify>
    - `npm run build` succeeds
    - colors.mdx has >60 lines of content (not placeholder)
  </verify>
  <done>Colors page has complete warm/neutral/brand/text token documentation with hex values and usage examples</done>
</task>

<task type="auto">
  <name>Task 2: Populate Typography and Spacing pages</name>
  <files>content/docs/foundations/typography.mdx, content/docs/foundations/spacing.mdx</files>
  <action>
    **Typography page** — replace placeholder with full documentation:

    1. **Sistema Tipografico** intro
    2. **Funnel Display** section:
       - Role: Titles and component labels
       - Usage: H1-H5, dialog titles, card titles, nav labels, component labels
       - Weights: Regular (400), Bold (700)
       - CSS variable: `var(--font-funnel-display)`
       - Tailwind utility: `font-ds-display`
    3. **Ubuntu Sans** section:
       - Role: Body text
       - Usage: Paragraphs, descriptions, labels, tooltips, metadata
       - Weights: Regular (400), Medium (500), Semi-bold (600), Bold (700)
       - CSS variable: `var(--font-ubuntu-sans)`
       - Tailwind utility: `font-ds-body`
    4. **Ubuntu Sans Mono** section:
       - Role: Numbers and CTAs
       - Usage: Montos, balances, buttons (uppercase), tabs, OTP inputs, references
       - Weights: Regular (400), Medium (500), Bold (700)
       - CSS variable: `var(--font-ubuntu-sans-mono)`
       - Tailwind utility: `font-ds-mono`
    5. **Escala de tamanos** — type scale table:
       | Token | Size | Line Height | Usage |
       | xs | 12px | 16px | Captions, labels pequenos |
       | sm | 14px | 20px | Body secundario, tooltips |
       | base | 16px | 24px | Body principal |
       | lg | 18px | 28px | Subtitulos |
       | xl | 20px | 28px | H5 |
       | 2xl | 24px | 32px | H4 |
       | 3xl | 30px | 36px | H3 |
       | 4xl | 36px | 40px | H2 |
       | 5xl | 48px | 48px | H1 |
    6. **Convenciones** — uppercase for buttons/tabs (Ubuntu Sans Mono), tracking 0.6px for status tags

    **Spacing page** — replace placeholder with full documentation:

    1. **Escala de Espaciado** intro — 4px base unit system
    2. **Tokens** table with ALL 19 spacing tokens:

       | Token | CSS Variable | Value | Uso comun |
       | 0 | --ds-space-0 | 0px | Reset |
       | 1 | --ds-space-1 | 4px | Inline spacing, icon gaps |
       | 2 | --ds-space-2 | 8px | Compact padding, small gaps |
       | 3 | --ds-space-3 | 12px | Inner container gap (double-layer) |
       | 4 | --ds-space-4 | 16px | Standard padding, form field spacing |
       | 5 | --ds-space-5 | 20px | Section padding |
       | 6 | --ds-space-6 | 24px | Card padding, dialog padding |
       | 7 | --ds-space-7 | 28px | Large component padding |
       | 8 | --ds-space-8 | 32px | Section margins |
       | 9 | --ds-space-9 | 36px | Large gaps |
       | 10 | --ds-space-10 | 40px | Page section spacing |
       | 11 | --ds-space-11 | 44px | Component heights (buttons) |
       | 12 | --ds-space-12 | 48px | Large section spacing |
       | 14 | --ds-space-14 | 56px | Extra large spacing |
       | 16 | --ds-space-16 | 64px | Maximum spacing token |
       (Add intermediate values 1.5=6px, 2.5=10px, 3.5=14px, 4.5=18px if they exist in the system)

    3. **Uso en codigo** — code example with CSS variables
    4. **Principios** — when to use which spacing level

    Content in Spanish.
  </action>
  <verify>
    - `npm run build` succeeds
    - typography.mdx has >60 lines
    - spacing.mdx has >40 lines
  </verify>
  <done>Typography page has all 3 fonts with roles/sizes/weights. Spacing page has all 19 tokens with visual scale.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Colors page has warm/neutral/brand sections with hex values
- [ ] Typography page has all 3 fonts documented with sizes and weights
- [ ] Spacing page has all spacing tokens listed
- [ ] No TypeScript or MDX errors
</verification>

<success_criteria>

- All tasks completed
- Three placeholder pages replaced with full content
- Build succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/06-foundations-content/06-01-SUMMARY.md`
</output>
