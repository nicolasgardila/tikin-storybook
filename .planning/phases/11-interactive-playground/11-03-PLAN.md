---
phase: 11-interactive-playground
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - components/ds/ds-dialog.tsx
  - components/ds/ds-toast.tsx
  - components/ds/ds-alert.tsx
  - components/ds/ds-tabs.tsx
  - components/ds/playgrounds/dialog-playground.tsx
  - components/ds/playgrounds/toast-playground.tsx
  - components/ds/playgrounds/alert-playground.tsx
  - components/ds/playgrounds/tabs-playground.tsx
  - mdx-components.tsx
  - content/docs/components/dialog.mdx
  - content/docs/components/toast.mdx
  - content/docs/components/alert.mdx
  - content/docs/components/tabs.mdx
autonomous: true
must_haves:
  truths:
    - "Dialog, Toast, Alert, Tabs each have a DS component and interactive playground"
    - "Each playground has controls matching the component's props"
    - "Controls update the live preview in real time"
    - "Total of 10 components now have interactive playgrounds (combined with Plans 01+02)"
  artifacts:
    - path: "components/ds/ds-dialog.tsx"
      provides: "DS Dialog with double-layer pattern"
      min_lines: 30
    - path: "components/ds/ds-alert.tsx"
      provides: "DS Alert with semantic variants"
      min_lines: 30
    - path: "components/ds/ds-tabs.tsx"
      provides: "DS Tabs with contained/underline variants"
      min_lines: 30
  key_links:
    - from: "content/docs/components/dialog.mdx"
      to: "components/ds/playgrounds/dialog-playground.tsx"
      via: "DialogPlayground MDX component"
      pattern: "DialogPlayground"
---

<objective>
Build 4 DS components (Dialog, Toast, Alert, Tabs) with playground wrappers, completing the 10-component playground target.

Purpose: Complete the interactive playground with overlay, feedback, and navigation components.
Output: 4 DS components, 4 playground wrappers, 4 MDX pages updated with playground sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-interactive-playground/11-01-SUMMARY.md
@components/ds/ds-button.tsx
@components/docs/component-playground.tsx
@components/docs/playground-controls.tsx
@mdx-components.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DSDialog and DSToast components + playgrounds</name>
  <files>components/ds/ds-dialog.tsx, components/ds/ds-toast.tsx, components/ds/playgrounds/dialog-playground.tsx, components/ds/playgrounds/toast-playground.tsx</files>
  <action>
    **DSDialog** (`components/ds/ds-dialog.tsx`):
    Props: title (string), description (string), size ('sm'|'md'|'lg'), showCloseButton (boolean), variant ('confirmation'|'destructive')
    - Double-layer: outer shell (bg --preview-muted-bg, border --preview-border, r24, p12) + inner panel (bg --preview-surface, r20, p36)
    - Title: Funnel Display font, centered or left
    - Description: Ubuntu Sans, secondary opacity
    - Bottom: two buttons (Primary + Secondary) using DSButton pattern
    - Destructive variant: primary button in red (#DC2626)
    - Shadow: 0 0 2px rgba(11,15,16,0.12)
    - Render as static card (no modal overlay in playground)

    Playground controls: title (text), description (text), size (select), showCloseButton (boolean), variant (select)

    **DSToast** (`components/ds/ds-toast.tsx`):
    Props: message (string), variant ('info'|'success'|'error'|'warning'), position ('top-right'|'bottom-center'), showClose (boolean)
    - Dark bg: #0B0B0B (always dark, independent of preview theme)
    - Text: white, Ubuntu Sans 14px
    - Border radius: 12px
    - Max-width: 360px
    - Icon left based on variant (info=circle-i, success=check, error=x, warning=triangle)
    - Close X button on right if showClose
    - Variant accent: info=blue, success=green, error=red, warning=amber (as icon/stripe color)

    Playground controls: message (text), variant (select), position (select), showClose (boolean)

    Each playground wrapper follows ButtonsPlayground pattern.
  </action>
  <verify>
    - `npm run build` succeeds
    - DSDialog and DSToast components render correctly
    - Playground wrappers created
  </verify>
  <done>DSDialog and DSToast components and playgrounds created</done>
</task>

<task type="auto">
  <name>Task 2: Create DSAlert, DSTabs + playgrounds, register all, update MDX pages</name>
  <files>components/ds/ds-alert.tsx, components/ds/ds-tabs.tsx, components/ds/playgrounds/alert-playground.tsx, components/ds/playgrounds/tabs-playground.tsx, mdx-components.tsx, content/docs/components/dialog.mdx, content/docs/components/toast.mdx, content/docs/components/alert.mdx, content/docs/components/tabs.mdx</files>
  <action>
    **DSAlert** (`components/ds/ds-alert.tsx`):
    Props: variant ('info'|'success'|'warning'|'error'), title (string), description (string), dismissible (boolean)
    - Full width, padding 16px, radius 12px
    - Colored left border 3px:
      info=#2563EB, success=#059669, warning=#D97706, error=#DC2626
    - Background tint: info=#EFF6FF, success=#ECFDF5, warning=#FFFBEB, error=#FEF2F2
    - Icon left matching variant
    - Title: Funnel Display 14px bold, color matching variant
    - Description: Ubuntu Sans 14px, darker shade
    - Dismiss X button if dismissible

    Playground controls: variant (select), title (text), description (text), dismissible (boolean)

    **DSTabs** (`components/ds/ds-tabs.tsx`):
    Props: variant ('contained'|'underline'), tabs (string — comma-separated), activeTab (number), size ('sm'|'md')
    - Contained: container bg --preview-muted-bg, r12, active tab bg --preview-surface, r8
    - Underline: no container, border-bottom 2px, active tab color --preview-brand
    - Tab font: Ubuntu Sans Mono, uppercase, tracking 0.05em (contained) or Ubuntu Sans (underline)
    - Parse tabs string into array for rendering
    - activeTab is 0-indexed

    Playground controls: variant (select), tabs (text, default "Tab 1, Tab 2, Tab 3"), activeTab (number, min 0, max 4), size (select)

    **Register ALL 4 playground components in mdx-components.tsx:**
    - DialogPlayground, ToastPlayground, AlertPlayground, TabsPlayground

    **Add Playground sections to 4 MDX pages:**
    For dialog.mdx, toast.mdx, alert.mdx, tabs.mdx:
    - Read each file
    - Add `## Playground` section after intro, before first variant
    - `<{Component}Playground />`

    IMPORTANT: Read mdx-components.tsx before editing — it was updated by Plan 02 in parallel.
  </action>
  <verify>
    - `npm run build` succeeds
    - All 4 playgrounds registered in mdx-components.tsx
    - All 4 MDX pages have Playground section
    - Total: 10 components with playgrounds (Button + 5 from Plan 02 + 4 here)
  </verify>
  <done>DSAlert, DSTabs created. All 4 playgrounds registered and integrated. 10 total components with playgrounds.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] 4 DS components created (Dialog, Toast, Alert, Tabs)
- [ ] 4 playground wrappers created
- [ ] All registered in mdx-components.tsx
- [ ] All 4 MDX pages have Playground section
- [ ] 10 total components now have interactive playgrounds
</verification>

<success_criteria>

- All tasks completed
- 4 overlay/feedback/nav components have interactive playgrounds
- 10 total playground components across all plans
- Build succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/11-interactive-playground/11-03-SUMMARY.md`
</output>
