---
phase: 11-interactive-playground
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - components/ds/ds-text-field.tsx
  - components/ds/ds-select.tsx
  - components/ds/ds-checkbox.tsx
  - components/ds/ds-toggle.tsx
  - components/ds/ds-card.tsx
  - components/ds/playgrounds/text-field-playground.tsx
  - components/ds/playgrounds/select-playground.tsx
  - components/ds/playgrounds/checkbox-playground.tsx
  - components/ds/playgrounds/toggle-playground.tsx
  - components/ds/playgrounds/card-playground.tsx
  - mdx-components.tsx
  - content/docs/components/text-field.mdx
  - content/docs/components/select.mdx
  - content/docs/components/checkbox.mdx
  - content/docs/components/toggle.mdx
  - content/docs/components/card.mdx
autonomous: true
must_haves:
  truths:
    - "TextField, Select, Checkbox, Toggle, Card each have a DS component and interactive playground"
    - "Each playground has controls matching the component's props"
    - "Controls update the live preview in real time"
  artifacts:
    - path: "components/ds/ds-text-field.tsx"
      provides: "DS TextField with floating label"
      min_lines: 30
    - path: "components/ds/ds-toggle.tsx"
      provides: "DS Toggle switch"
      min_lines: 20
    - path: "components/ds/ds-card.tsx"
      provides: "DS Card with variants"
      min_lines: 30
  key_links:
    - from: "content/docs/components/text-field.mdx"
      to: "components/ds/playgrounds/text-field-playground.tsx"
      via: "TextFieldPlayground MDX component"
      pattern: "TextFieldPlayground"
---

<objective>
Build 5 DS components (TextField, Select, Checkbox, Toggle, Card) with playground wrappers and integrate into their docs pages.

Purpose: Expand the interactive playground to the most important form and layout components.
Output: 5 DS components, 5 playground wrappers, 5 MDX pages updated with playground sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-interactive-playground/11-01-SUMMARY.md
@components/ds/ds-button.tsx
@components/docs/component-playground.tsx
@components/docs/playground-controls.tsx
@mdx-components.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DSTextField, DSSelect, DSCheckbox DS components + playgrounds</name>
  <files>components/ds/ds-text-field.tsx, components/ds/ds-select.tsx, components/ds/ds-checkbox.tsx, components/ds/playgrounds/text-field-playground.tsx, components/ds/playgrounds/select-playground.tsx, components/ds/playgrounds/checkbox-playground.tsx</files>
  <action>
    Create 3 DS components and their playground wrappers. Follow DSButton pattern from Plan 01.

    **DSTextField** (`components/ds/ds-text-field.tsx`):
    Props: size ('xs'|'sm'|'md'|'lg'), state ('default'|'active'|'disabled'), feedback ('default'|'success'|'error'), label (string), placeholder (string), showIcon (boolean)
    - Floating label pattern: label inside, shrinks to 10px when active
    - Size heights: xs=32, sm=40, md=48, lg=56
    - Size radii: xs=6, sm=8, md=10, lg=12
    - Uses --preview-surface, --preview-border for dark mode
    - Default state: label as Funnel Display placeholder
    - Active state: small label (Ubuntu Sans 10px) + placeholder below
    - Error: red border (#FF2E2E), red tinted bg

    Playground controls: size (select), state (select), feedback (select), label (text), placeholder (text), showIcon (boolean)

    **DSSelect** (`components/ds/ds-select.tsx`):
    Props: size ('sm'|'md'|'lg'), state ('default'|'active'|'disabled'), label (string), value (string)
    - Same border/radius as TextField
    - Shows trigger + chevron
    - When active, shows dropdown with options below
    - Uses --preview-surface, --preview-border

    Playground controls: size (select), state (select), label (text)

    **DSCheckbox** (`components/ds/ds-checkbox.tsx`):
    Props: checked (boolean), indeterminate (boolean), disabled (boolean), label (string), size ('sm'|'md')
    - 20×20 (md) or 16×16 (sm) box
    - Checked: bg --preview-brand, white checkmark SVG
    - Unchecked: bg --preview-surface, border --preview-border
    - Label: Ubuntu Sans, gap 8px

    Playground controls: checked (boolean), indeterminate (boolean), disabled (boolean), label (text), size (select)

    All components use inline styles with --preview-* CSS vars for dark mode. NOT 'use client' (pure presentational).

    Each playground wrapper is a 'use client' component that imports ComponentPlayground + the DS component + defines controls array. Follow ButtonsPlayground pattern exactly.
  </action>
  <verify>
    - `npm run build` succeeds
    - 3 DS components and 3 playground wrappers created
  </verify>
  <done>DSTextField, DSSelect, DSCheckbox components and playgrounds created</done>
</task>

<task type="auto">
  <name>Task 2: Create DSToggle, DSCard + playgrounds, register all, update MDX pages</name>
  <files>components/ds/ds-toggle.tsx, components/ds/ds-card.tsx, components/ds/playgrounds/toggle-playground.tsx, components/ds/playgrounds/card-playground.tsx, mdx-components.tsx, content/docs/components/text-field.mdx, content/docs/components/select.mdx, content/docs/components/checkbox.mdx, content/docs/components/toggle.mdx, content/docs/components/card.mdx</files>
  <action>
    **1. Create DSToggle and DSCard:**

    **DSToggle** (`components/ds/ds-toggle.tsx`):
    Props: checked (boolean), disabled (boolean), label (string), size ('sm'|'md')
    - Track: 44×24 (md) or 36×20 (sm), rounded-full
    - Off: track bg --preview-border, thumb bg --preview-surface
    - On: track bg --preview-brand, thumb bg white
    - Thumb: 20×20 (md) or 16×16 (sm), 2px inset
    - Transition: 200ms ease
    - Label: Ubuntu Sans 16px, gap 12px

    Playground controls: checked (boolean), disabled (boolean), label (text), size (select)

    **DSCard** (`components/ds/ds-card.tsx`):
    Props: variant ('default'|'neutral'|'filled'|'elevated'|'outline'), padding ('default'|'compact'), children (ReactNode)
    - Default: bg --preview-surface, border 0.5px warm (--preview-border)
    - Neutral: same but neutral border
    - Filled: bg --preview-muted-bg
    - Elevated: no border, shadow
    - Outline: transparent bg, border
    - Default padding: 20px, compact: 16px
    - Radius: 24px
    - Render children or sample content if no children

    Playground controls: variant (select), padding (select)

    **2. Register ALL 5 playground components in mdx-components.tsx:**

    Read current file first. Add imports and registrations for:
    - TextFieldPlayground
    - SelectPlayground
    - CheckboxPlayground
    - TogglePlayground
    - CardPlayground

    **3. Add Playground sections to 5 MDX pages:**

    For each of text-field.mdx, select.mdx, checkbox.mdx, toggle.mdx, card.mdx:
    - Read the file
    - Add a `## Playground` section after the StatusBadge + intro, BEFORE the first variant section
    - Content:
      ```mdx
      ## Playground

      Explora todas las propiedades del componente en tiempo real.

      <{Component}Playground />
      ```
  </action>
  <verify>
    - `npm run build` succeeds
    - All 5 playgrounds registered in mdx-components.tsx
    - All 5 MDX pages have Playground section
  </verify>
  <done>DSToggle, DSCard created. All 5 playgrounds registered and integrated into MDX pages.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] 5 DS components created (TextField, Select, Checkbox, Toggle, Card)
- [ ] 5 playground wrappers created
- [ ] All registered in mdx-components.tsx
- [ ] All 5 MDX pages have Playground section
</verification>

<success_criteria>

- All tasks completed
- 5 form/layout components have interactive playgrounds
- Build succeeds
  </success_criteria>

<output>
After completion, create `.planning/phases/11-interactive-playground/11-02-SUMMARY.md`
</output>
