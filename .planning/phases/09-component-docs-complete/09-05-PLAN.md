---
phase: 09-component-docs-complete
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - content/docs/components/alert.mdx
  - content/docs/components/chip.mdx
  - content/docs/components/date-picker.mdx
  - content/docs/components/slider.mdx
  - content/docs/components/snackbar.mdx
  - content/docs/components/meta.json
autonomous: true
must_haves:
  truths:
    - "Alert/Banner page has ComponentPreview with inline styles, code examples, and PropsTable"
    - "Chip/Tag page has ComponentPreview with inline styles, code examples, and PropsTable"
    - "Date Picker page has ComponentPreview with inline styles, code examples, and PropsTable"
    - "Slider/Range page has ComponentPreview with inline styles, code examples, and PropsTable"
    - "Snackbar page has ComponentPreview with inline styles, code examples, and PropsTable"
    - "meta.json includes all 36 component page entries (index + 12 existing + 23 new)"
  artifacts:
    - path: "content/docs/components/alert.mdx"
      provides: "Alert/Banner documentation with live previews"
      min_lines: 60
      contains: "ComponentPreview"
    - path: "content/docs/components/chip.mdx"
      provides: "Chip/Tag documentation with live previews"
      min_lines: 60
      contains: "ComponentPreview"
    - path: "content/docs/components/date-picker.mdx"
      provides: "Date Picker documentation with live previews"
      min_lines: 60
      contains: "ComponentPreview"
    - path: "content/docs/components/slider.mdx"
      provides: "Slider/Range documentation with live previews"
      min_lines: 60
      contains: "ComponentPreview"
    - path: "content/docs/components/snackbar.mdx"
      provides: "Snackbar documentation with live previews"
      min_lines: 60
      contains: "ComponentPreview"
    - path: "content/docs/components/meta.json"
      provides: "Complete sidebar with all 36 component entries"
      min_lines: 30
      contains: "snackbar"
  key_links: []
---

<objective>
Document Alert/Banner, Chip/Tag, Date Picker, Slider/Range, and Snackbar components, then update meta.json with all 36 component page entries.

Purpose: Complete the remaining components and finalize the sidebar navigation for all documented components.
Output: Five MDX component pages plus updated meta.json with all entries.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@content/docs/components/buttons.mdx
@content/docs/components/meta.json
@app/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Alert, Chip, and Date Picker</name>
  <files>content/docs/components/alert.mdx, content/docs/components/chip.mdx, content/docs/components/date-picker.mdx</files>
  <action>
    Create three new component documentation pages. Follow the Buttons page pattern (read buttons.mdx first).

    **alert.mdx**:
    ```
    ---
    title: Alert
    description: Banner de alerta con borde lateral, icono y texto para mensajes informativos, exito, advertencia y error
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro (banners para comunicar estados y mensajes importantes)
    2. **Info** variant with ComponentPreview:
       - Full-width, padding 16px, border-radius 12px
       - Left border: 3px solid #3B82F6 (blue)
       - Background: #EFF6FF (light blue)
       - Icon (inline SVG info circle) + text
       - Optional dismiss X button on right
    3. **Success** variant — left border #22C55E, bg #F0FDF4
    4. **Warning** variant — left border #F59E0B, bg #FFFBEB
    5. **Error** variant — left border #E74C3C, bg #FEF2F2
    6. Code blocks for each variant
    7. PropsTable: variant ('info' | 'success' | 'warning' | 'error'), title, message, dismissible, onDismiss, icon
    8. DoDont:
       - Do: Usar el color y el icono apropiado para cada tipo de mensaje
       - Don't: No usar alerts para informacion de flujo normal, reservarlos para estados excepcionales

    Key specs:
    - Padding: 16px
    - Border-radius: 12px
    - Left border: 3px solid (color varies by variant)
    - Icon: 20px, margin-right 12px
    - Title: Ubuntu Sans, 14px, font-weight 600
    - Message: Ubuntu Sans, 14px, color #666666
    - Dismiss button: 16px X, top-right

    **chip.mdx**:
    ```
    ---
    title: Chip
    description: Etiquetas interactivas con variantes de color y boton de eliminar
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro (etiquetas compactas para categorias, filtros, o selecciones)
    2. **Default** with ComponentPreview:
       - Small pill shape, border-radius 9999px
       - bg #F6F6F6, text #0B0B0B
       - Padding: 4px 12px
       - Font: Ubuntu Sans, 12px, font-weight 500
    3. **With Close Button** — X icon on the right for removable chips
    4. **Selected State** — bg #0B0B0B, text white
    5. **Color Variants** — info (blue), success (green), warning (amber), error (red)
    6. Code blocks for each
    7. PropsTable: label, variant ('default' | 'info' | 'success' | 'warning' | 'error'), selected, removable, onRemove, onClick, size ('sm' | 'md')
    8. DoDont:
       - Do: Usar chips para filtros activos, tags, o selecciones multiples compactas
       - Don't: No usar chips como botones de accion principal, son para metadata y categorias

    Key specs:
    - Border-radius: 9999px (full pill)
    - Padding: 4px 12px (md), 2px 8px (sm)
    - Font: Ubuntu Sans, 12px, font-weight 500
    - Default: bg #F6F6F6, text #0B0B0B
    - Selected: bg #0B0B0B, text #FFFFFF
    - Close icon: 12px, margin-left 4px
    - Height: 28px (md), 22px (sm)

    **date-picker.mdx**:
    ```
    ---
    title: Date Picker
    description: Selector de fecha con calendario desplegable, navegacion por mes y seleccion visual
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro (calendario desplegable para seleccionar fechas)
    2. **Calendar Grid** with ComponentPreview:
       - Show the calendar in "open" state statically
       - Header: month/year label centered, prev/next chevron arrows
       - 7 columns (L, M, Mi, J, V, S, D), day abbreviations as header
       - Today: border 1px solid #0B0B0B
       - Selected day: bg #0B0B0B, text white, border-radius 50%
       - Other days: transparent bg, hover bg #F6F6F6
       - Day cell: 36x36px, centered text
    3. **Input Trigger** — text field that opens the calendar (show trigger + calendar)
    4. **Date Range** — two dates selected with range highlighted
    5. Code blocks for each variant
    6. PropsTable: value, onChange, minDate, maxDate, locale, placeholder, format, range, disabled
    7. DoDont:
       - Do: Mostrar el mes actual por defecto y resaltar la fecha de hoy
       - Don't: No desactivar fechas sin indicar claramente por que no estan disponibles

    Key specs:
    - Calendar container: bg white, border 1px solid #DEDEDE, border-radius 12px, shadow-md, padding 16px
    - Header: font-weight 600, 16px, month/year centered
    - Nav arrows: 20px chevron icons
    - Day cell: 36x36px, font 14px, centered
    - Selected: bg #0B0B0B, text #FFFFFF, border-radius 50%
    - Today: border 1px solid #0B0B0B
    - Hover: bg #F6F6F6
    - Day header: Ubuntu Sans, 12px, color #999999, font-weight 500
    - Range highlight: bg #F6F6F6 between selected dates

    All ComponentPreview content uses inline styles. Content in Spanish.
  </action>
  <verify>
    - `npm run build` succeeds
    - alert.mdx, chip.mdx, date-picker.mdx exist with >60 lines each
    - All contain ComponentPreview
  </verify>
  <done>Alert, Chip, and Date Picker pages created with live previews, code examples, PropsTable, and DoDont</done>
</task>

<task type="auto">
  <name>Task 2: Document Slider, Snackbar, and update meta.json</name>
  <files>content/docs/components/slider.mdx, content/docs/components/snackbar.mdx, content/docs/components/meta.json</files>
  <action>
    Create two new component documentation pages and update meta.json. Follow the same pattern as buttons.mdx.

    **slider.mdx**:
    ```
    ---
    title: Slider
    description: Control deslizante para seleccionar valores dentro de un rango con track y thumb
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro (seleccion de valor numerico dentro de un rango definido)
    2. **Default** with ComponentPreview:
       - Track: bg #F6F6F6, height 8px (h-2), border-radius 9999px, full width
       - Fill (left of thumb): bg #0B0B0B, border-radius 9999px
       - Thumb: 20x20px circle, bg white, border 2px solid #0B0B0B
       - Show at ~40% position
    3. **With Labels** — min/max labels at ends, current value above thumb
    4. **Range** variant — two thumbs for range selection
    5. **Disabled** — muted colors
    6. Code blocks for each variant
    7. PropsTable: value, min, max, step, onChange, disabled, showLabels, range, formatValue
    8. DoDont:
       - Do: Mostrar el valor actual y los limites del rango para que el usuario sepa el contexto
       - Don't: No usar slider para valores exactos donde precision es critica, usar input numerico

    Key specs:
    - Track: bg #F6F6F6, height 8px, border-radius 9999px
    - Fill: bg #0B0B0B, border-radius 9999px
    - Thumb: 20x20px circle, bg #FFFFFF, border 2px solid #0B0B0B
    - Thumb hover: shadow 0 0 0 4px rgba(11,11,11,0.1)
    - Label font: Ubuntu Sans, 12px, color #999999
    - Value font: Ubuntu Sans, 14px, font-weight 600
    - Disabled: opacity 0.5

    **snackbar.mdx**:
    ```
    ---
    title: Snackbar
    description: Notificacion temporal en la parte inferior de la pantalla con accion opcional
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro (mensaje breve que aparece temporalmente, similar a toast pero posicionado abajo al centro)
    2. **Default** with ComponentPreview:
       - Position: bottom center of container
       - bg #0B0B0B, text white, border-radius 12px
       - Padding: 12px 16px
       - Font: Ubuntu Sans, 14px
       - Sample text: "Elemento eliminado"
    3. **With Action** — text + action button ("Deshacer") on right
       - Action: text button, uppercase, font-weight 600, color brand or white
    4. **With Icon** — icon on left + message
    5. Code blocks for each variant
    6. PropsTable: message, action, onAction, duration (ms), position ('bottom-center' | 'bottom-left' | 'bottom-right'), icon, onDismiss
    7. DoDont:
       - Do: Usar para confirmaciones de acciones con opcion de deshacer (eliminar, archivar)
       - Don't: No usar para mensajes criticos que requieren atencion obligatoria (usar Alert o Dialog)

    Key specs:
    - Background: #0B0B0B
    - Text: #FFFFFF, Ubuntu Sans 14px
    - Border-radius: 12px
    - Padding: 12px 16px
    - Action button: uppercase, font-weight 600, 12px, padding 4px 8px
    - Max-width: 400px
    - Min-width: 280px
    - Auto-dismiss: 4000ms default
    - Z-index: 60
    - Shadow: 0 4px 12px rgba(0,0,0,0.15)

    **Update meta.json**:
    Read the current meta.json, then update it to include ALL component pages.
    The final pages array must include (in this order):
    ```json
    {
      "title": "Components",
      "defaultOpen": false,
      "pages": [
        "index",
        "buttons",
        "text-field",
        "select",
        "checkbox",
        "toggle",
        "card",
        "container",
        "badge",
        "status-chips",
        "toast",
        "dialog",
        "dropdown",
        "radio-button",
        "tabs",
        "search-bar",
        "otp-input",
        "segmented-control",
        "avatar",
        "tooltip",
        "skeleton",
        "divider",
        "progress-bar",
        "bottom-sheet",
        "side-panel",
        "accordion",
        "popover",
        "table",
        "pagination",
        "breadcrumbs",
        "navigation",
        "alert",
        "chip",
        "date-picker",
        "slider",
        "snackbar"
      ]
    }
    ```
    That is 36 entries total (1 index + 12 existing + 23 new).

    All ComponentPreview content uses inline styles. Content in Spanish.
  </action>
  <verify>
    - `npm run build` succeeds
    - slider.mdx and snackbar.mdx exist with >60 lines each
    - Both contain ComponentPreview
    - meta.json has 36 entries in pages array
  </verify>
  <done>Slider and Snackbar pages created, meta.json updated with all 36 component entries</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] All five component pages created
- [ ] All five have ComponentPreview, code blocks, PropsTable, DoDont
- [ ] meta.json has 36 entries (index + 12 Phase 8 + 23 Phase 9)
- [ ] Content is in Spanish
</verification>

<success_criteria>

- All tasks completed
- Five remaining components documented
- meta.json updated with complete sidebar entries
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/09-component-docs-complete/09-05-SUMMARY.md`
</output>
