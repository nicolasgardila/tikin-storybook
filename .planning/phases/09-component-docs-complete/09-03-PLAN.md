---
phase: 09-component-docs-complete
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - content/docs/components/bottom-sheet.mdx
  - content/docs/components/side-panel.mdx
  - content/docs/components/accordion.mdx
  - content/docs/components/popover.mdx
autonomous: true
must_haves:
  truths:
    - "Bottom Sheet page has ComponentPreview with inline styles, code examples, and PropsTable"
    - "Side Panel page has ComponentPreview with inline styles, code examples, and PropsTable"
    - "Accordion page has ComponentPreview with inline styles, code examples, and PropsTable"
    - "Popover page has ComponentPreview with inline styles, code examples, and PropsTable"
  artifacts:
    - path: "content/docs/components/bottom-sheet.mdx"
      provides: "Bottom Sheet documentation with live previews"
      min_lines: 60
      contains: "ComponentPreview"
    - path: "content/docs/components/side-panel.mdx"
      provides: "Side Panel documentation with live previews"
      min_lines: 60
      contains: "ComponentPreview"
    - path: "content/docs/components/accordion.mdx"
      provides: "Accordion documentation with live previews"
      min_lines: 60
      contains: "ComponentPreview"
    - path: "content/docs/components/popover.mdx"
      provides: "Popover/Dropdown Menu documentation with live previews"
      min_lines: 60
      contains: "ComponentPreview"
  key_links: []
---

<objective>
Document Bottom Sheet, Side Panel, Accordion, and Popover/Dropdown Menu components with live previews, code examples, and props tables.

Purpose: Create comprehensive documentation for overlay and expandable components.
Output: Four MDX component pages following the established Buttons page template pattern.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@content/docs/components/buttons.mdx
@app/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Bottom Sheet and Side Panel</name>
  <files>content/docs/components/bottom-sheet.mdx, content/docs/components/side-panel.mdx</files>
  <action>
    Create two new component documentation pages. Follow the Buttons page pattern (read buttons.mdx first).

    **bottom-sheet.mdx**:
    ```
    ---
    title: Bottom Sheet
    description: Panel deslizable desde abajo con drag handle para acciones y contenido contextual
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro (panel que se desliza desde la parte inferior, comun en mobile)
    2. **Default** with ComponentPreview:
       - Show the sheet in "open" state statically (no animation needed in preview)
       - Container: bg white, border-radius 24px on top corners, padding 16px
       - Drag handle bar: 32x4px, bg #DEDEDE, border-radius full, centered at top, margin-bottom 16px
       - Content area with sample text and a button
       - Overlay background: rgba(0,0,0,0.4) behind the sheet
    3. **With Actions** variant — sheet with title + list items or buttons
    4. **Sizes** — compact (auto height) vs full (80vh)
    5. Code blocks for each variant
    6. PropsTable: open, onClose, title, size ('auto' | 'full'), showHandle, children
    7. DoDont:
       - Do: Usar el drag handle para permitir cerrar con gesto de swipe hacia abajo
       - Don't: No poner contenido critico que el usuario pueda perder al cerrar accidentalmente

    Key specs:
    - Background: #FFFFFF
    - Top border-radius: 24px
    - Drag handle: 32x4px, bg #DEDEDE, border-radius 9999px, centered
    - Padding: 16px
    - Overlay: rgba(0,0,0,0.4)
    - Max-height: 90vh
    - Z-index: 50

    **side-panel.mdx**:
    ```
    ---
    title: Side Panel
    description: Panel lateral fijo para contenido secundario, filtros o configuracion
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro (panel lateral que se abre desde la derecha)
    2. **Default** with ComponentPreview:
       - Show panel in "open" state statically
       - Fixed right side, width 360px, full height
       - Background: #FAF8F4 (warm), padding 24px
       - Header with title and close X button
       - Overlay: rgba(0,0,0,0.4)
    3. **With Form Content** — panel containing form fields
    4. **Narrow vs Wide** — width 320px vs 480px
    5. Code blocks for each variant
    6. PropsTable: open, onClose, title, width (320 | 360 | 480), position ('right' | 'left'), children
    7. DoDont:
       - Do: Usar para contenido secundario que no requiere una pagina nueva (filtros, detalles, config)
       - Don't: No usar Side Panel para flujos principales o formularios largos que merecen su propia vista

    Key specs:
    - Position: fixed right (default)
    - Width: 320-480px (default 360px)
    - Background: #FAF8F4 (warm tone)
    - Height: 100vh
    - Padding: 24px
    - Overlay: rgba(0,0,0,0.4)
    - Z-index: 50
    - Border-left: 1px solid #E8E4DD
    - Close button: top-right, 24px icon

    All ComponentPreview content uses inline styles. Content in Spanish.
  </action>
  <verify>
    - `npm run build` succeeds
    - bottom-sheet.mdx and side-panel.mdx exist with >60 lines each
    - Both contain ComponentPreview
  </verify>
  <done>Bottom Sheet and Side Panel pages created with live previews, code examples, PropsTable, and DoDont</done>
</task>

<task type="auto">
  <name>Task 2: Document Accordion and Popover</name>
  <files>content/docs/components/accordion.mdx, content/docs/components/popover.mdx</files>
  <action>
    Create two new component documentation pages. Follow the same pattern as buttons.mdx.

    **accordion.mdx**:
    ```
    ---
    title: Accordion
    description: Secciones colapsables con chevron rotativo para contenido expandible
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro (secciones que se expanden y colapsan para organizar contenido)
    2. **Single Item (collapsed)** with ComponentPreview:
       - Header with text + chevron icon on right, padding 16px
       - Border-bottom: 1px solid #DEDEDE
       - Chevron: rotates 180deg when expanded
    3. **Single Item (expanded)** — content visible below header
    4. **Multiple Items** — 3 accordion items, one expanded
    5. Code blocks for each
    6. PropsTable: items, defaultOpen, allowMultiple, onChange
    7. Item sub-props: title, content, disabled
    8. DoDont:
       - Do: Usar para FAQs, configuraciones agrupadas, o contenido largo que se puede segmentar
       - Don't: No ocultar informacion critica dentro de un accordion que el usuario debe ver siempre

    Key specs:
    - Border-bottom: 1px solid #DEDEDE
    - Padding: 16px (header and content)
    - Chevron: 16px, color #999999, transition rotate 200ms ease
    - Header font: Ubuntu Sans, 16px, font-weight 500
    - Content font: Ubuntu Sans, 14px, color #666666
    - Content animation: height auto with transition (or max-height trick)

    **popover.mdx**:
    ```
    ---
    title: Popover
    description: Menu flotante con opciones contextuales que aparece junto al elemento trigger
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro (menu contextual que aparece cerca del trigger, usado como dropdown menu)
    2. **Default** with ComponentPreview:
       - Show popover in "open" state statically near a trigger button
       - bg white, border 1px solid #DEDEDE, border-radius 12px
       - Shadow: 0 4px 12px rgba(0,0,0,0.1)
       - List of menu items with hover state
    3. **With Sections** — divider between groups of items
    4. **With Icons** — items with inline SVG icons
    5. Code blocks for each
    6. PropsTable: open, onClose, trigger, position ('top' | 'bottom' | 'left' | 'right'), align ('start' | 'center' | 'end'), children
    7. MenuItem sub-props: label, icon, onClick, disabled, destructive
    8. DoDont:
       - Do: Usar para menus contextuales con 3-8 opciones relacionadas
       - Don't: No poner formularios o contenido complejo dentro de un popover

    Key specs:
    - Background: #FFFFFF
    - Border: 1px solid #DEDEDE
    - Border-radius: 12px
    - Shadow: 0 4px 12px rgba(0,0,0,0.1)
    - Min-width: 160px, max-width: 280px
    - Menu item padding: 8px 12px
    - Menu item hover: bg #F6F6F6
    - Destructive item: color #E74C3C
    - Font: Ubuntu Sans, 14px
    - Z-index: 50

    All ComponentPreview content uses inline styles. Content in Spanish.
  </action>
  <verify>
    - `npm run build` succeeds
    - accordion.mdx and popover.mdx exist with >60 lines each
    - Both contain ComponentPreview
  </verify>
  <done>Accordion and Popover pages created with live previews, code examples, PropsTable, and DoDont</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] All four component pages created
- [ ] All four have ComponentPreview, code blocks, PropsTable, DoDont
- [ ] Content is in Spanish
</verification>

<success_criteria>

- All tasks completed
- Four overlay/expandable components documented
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/09-component-docs-complete/09-03-SUMMARY.md`
</output>
