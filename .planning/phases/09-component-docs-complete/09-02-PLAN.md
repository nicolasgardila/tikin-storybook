---
phase: 09-component-docs-complete
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - content/docs/components/avatar.mdx
  - content/docs/components/tooltip.mdx
  - content/docs/components/skeleton.mdx
  - content/docs/components/divider.mdx
  - content/docs/components/progress-bar.mdx
autonomous: true
must_haves:
  truths:
    - "Avatar page has ComponentPreview with inline styles, code examples, and PropsTable"
    - "Tooltip page has ComponentPreview with inline styles, code examples, and PropsTable"
    - "Skeleton page has ComponentPreview with inline styles, code examples, and PropsTable"
    - "Divider page has ComponentPreview with inline styles, code examples, and PropsTable"
    - "Progress Bar page has ComponentPreview showing bar and step indicator variants with code and PropsTable"
  artifacts:
    - path: "content/docs/components/avatar.mdx"
      provides: "Avatar documentation with live previews"
      min_lines: 60
      contains: "ComponentPreview"
    - path: "content/docs/components/tooltip.mdx"
      provides: "Tooltip documentation with live previews"
      min_lines: 60
      contains: "ComponentPreview"
    - path: "content/docs/components/skeleton.mdx"
      provides: "Skeleton documentation with live previews"
      min_lines: 60
      contains: "ComponentPreview"
    - path: "content/docs/components/divider.mdx"
      provides: "Divider documentation with live previews"
      min_lines: 60
      contains: "ComponentPreview"
    - path: "content/docs/components/progress-bar.mdx"
      provides: "Progress Bar and Step Indicator documentation with live previews"
      min_lines: 60
      contains: "ComponentPreview"
  key_links: []
---

<objective>
Document Avatar, Tooltip, Skeleton, Divider, and Progress Bar / Step Indicator components with live previews, code examples, and props tables.

Purpose: Create comprehensive documentation for display and feedback components.
Output: Five MDX component pages following the established Buttons page template pattern.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@content/docs/components/buttons.mdx
@app/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Avatar, Tooltip, and Skeleton</name>
  <files>content/docs/components/avatar.mdx, content/docs/components/tooltip.mdx, content/docs/components/skeleton.mdx</files>
  <action>
    Create three new component documentation pages. Follow the Buttons page pattern (read buttons.mdx first).

    **avatar.mdx**:
    ```
    ---
    title: Avatar
    description: Avatar circular con imagen, iniciales o fallback y indicador de estado
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro (avatar circular con multiples tamanos y fallbacks)
    2. **With Initials** variant with ComponentPreview:
       - Circle (border-radius 50%), bg #F6F6F6, centered initials text
       - Show sizes: 24px, 32px, 40px, 48px, 64px in a row
    3. **With Status Indicator** — small colored dot (green online, gray offline) positioned bottom-right
    4. **Sizes** — all 5 sizes side by side
    5. Code blocks for each variant
    6. PropsTable: src, alt, initials, size (24 | 32 | 40 | 48 | 64), status ('online' | 'offline' | 'away'), fallback
    7. DoDont:
       - Do: Siempre proporcionar initials como fallback cuando la imagen puede no cargar
       - Don't: No usar avatares menores a 24px, la legibilidad se pierde

    Key specs:
    - Sizes: 24/32/40/48/64px
    - Border-radius: 50% (circle)
    - Initials: Ubuntu Sans, font-weight 600, centered
    - Font sizes for initials: 10/12/14/16/20px (matching avatar sizes)
    - Status dot: 8px circle, border 2px solid white, positioned absolute bottom-right
    - Background (no image): #F6F6F6
    - Text color: #0B0B0B

    **tooltip.mdx**:
    ```
    ---
    title: Tooltip
    description: Tooltip flotante con texto informativo al hacer hover sobre un elemento
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro (informacion contextual al hacer hover)
    2. **Default (top)** with ComponentPreview:
       - bg #0B0B0B, text white, border-radius 8px, padding 8px 12px
       - Arrow/caret pointing down toward trigger
       - Max-width: 200px, font-size 12px
       - Show tooltip statically visible above a button trigger
    3. **Positions** — top, bottom, left, right (show all four)
    4. Code blocks for each
    5. PropsTable: content, position ('top' | 'bottom' | 'left' | 'right'), trigger, delay, maxWidth
    6. DoDont:
       - Do: Usar para texto breve y complementario que no es esencial para la tarea
       - Don't: No poner contenido critico o acciones dentro de un tooltip

    Key specs:
    - Background: #0B0B0B
    - Text: #FFFFFF, 12px, Ubuntu Sans
    - Border-radius: 8px
    - Padding: 8px 12px
    - Max-width: 200px
    - Arrow: 6px CSS triangle
    - Z-index: 50

    **skeleton.mdx**:
    ```
    ---
    title: Skeleton
    description: Placeholder animado para contenido en carga con formas de texto, circulo y rectangulo
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro (placeholder visual mientras el contenido carga)
    2. **Text Lines** with ComponentPreview:
       - Rectangles bg #F6F6F6, border-radius 8px
       - Pulse animation (opacity 0.5 to 1, 1.5s infinite)
       - Show 3 lines of varying widths (100%, 80%, 60%)
    3. **Circle** variant — for avatars, border-radius 50%
    4. **Card Skeleton** — composition of circle + text lines simulating a card
    5. Code blocks for each
    6. PropsTable: variant ('text' | 'circle' | 'rectangle'), width, height, lines, animate
    7. DoDont:
       - Do: Usar skeletons que reflejen la estructura del contenido real que reemplazaran
       - Don't: No usar skeletons para contenido que carga instantaneamente (menos de 200ms)

    Key specs:
    - Background: #F6F6F6
    - Border-radius: 8px (text/rectangle), 50% (circle)
    - Animation: pulse, opacity 0.5 to 1.0, duration 1.5s, infinite
    - Text line height: 16px, gap 8px between lines
    - Circle sizes: match Avatar sizes (32, 40, 48px)

    All ComponentPreview content uses inline styles. Content in Spanish.
  </action>
  <verify>
    - `npm run build` succeeds
    - avatar.mdx, tooltip.mdx, skeleton.mdx exist with >60 lines each
    - All contain ComponentPreview
  </verify>
  <done>Avatar, Tooltip, and Skeleton pages created with live previews, code examples, PropsTable, and DoDont</done>
</task>

<task type="auto">
  <name>Task 2: Document Divider and Progress Bar</name>
  <files>content/docs/components/divider.mdx, content/docs/components/progress-bar.mdx</files>
  <action>
    Create two new component documentation pages. Follow the same pattern as buttons.mdx.

    **divider.mdx**:
    ```
    ---
    title: Divider
    description: Separador horizontal con variantes neutral y warm y label opcional
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro (separacion visual entre secciones de contenido)
    2. **Neutral** variant with ComponentPreview:
       - hr element, 1px solid #DEDEDE, full width
    3. **Warm** variant:
       - hr element, 1px solid #E8E4DD
    4. **With Label** variant — text centered with lines on each side ("o", "Seccion")
    5. Code blocks for each
    6. PropsTable: variant ('neutral' | 'warm'), label, spacing ('sm' | 'md' | 'lg')
    7. DoDont:
       - Do: Usar el tono warm en fondos #FAF8F4, neutral en fondos blancos
       - Don't: No abusar de dividers, usar spacing suficiente antes de recurrir a lineas

    Key specs:
    - Neutral: 1px solid #DEDEDE
    - Warm: 1px solid #E8E4DD
    - Margin: 16px 0 (md), 8px 0 (sm), 24px 0 (lg)
    - Label: Ubuntu Sans 12px, color #999999, padding 0 12px, bg matching parent

    **progress-bar.mdx**:
    ```
    ---
    title: Progress Bar
    description: Barra de progreso lineal y step indicator con puntos para flujos multi-paso
    status: stable
    ---
    ```
    Content:
    1. StatusBadge + intro (dos variantes: barra continua y step indicator con puntos)
    2. **Progress Bar** with ComponentPreview:
       - Track: bg #F6F6F6, border-radius full (9999px), height 8px (h-2)
       - Fill: bg #0B0B0B, border-radius full, width percentage
       - Show at 60% progress
    3. **Progress Bar with Label** — percentage text above or to the right
    4. **Step Indicator** variant with ComponentPreview:
       - Dots: 12px circles connected by 2px lines
       - Completed: bg #0B0B0B
       - Current: bg #0B0B0B with ring/outline
       - Upcoming: bg #F6F6F6, border #DEDEDE
       - Show 4 steps, step 2 active
    5. Code blocks for each variant
    6. PropsTable: variant ('bar' | 'steps'), value (0-100 for bar), currentStep, totalSteps, showLabel, size
    7. DoDont:
       - Do: Usar progress bar para operaciones con progreso medible, step indicator para flujos con pasos definidos
       - Don't: No usar progress bar para flujos de duracion indeterminada (usar Skeleton o spinner)

    Key specs:
    - Track: bg #F6F6F6, border-radius 9999px, height 8px
    - Fill: bg #0B0B0B, border-radius 9999px
    - Step dot: 12px circle
    - Step line: 2px solid, connects dots
    - Completed step: bg #0B0B0B
    - Current step: bg #0B0B0B, ring 2px offset
    - Upcoming step: bg #F6F6F6, border 1.5px #DEDEDE

    All ComponentPreview content uses inline styles. Content in Spanish.
  </action>
  <verify>
    - `npm run build` succeeds
    - divider.mdx and progress-bar.mdx exist with >60 lines each
    - Both contain ComponentPreview
  </verify>
  <done>Divider and Progress Bar pages created with live previews, code examples, PropsTable, and DoDont</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] All five component pages created
- [ ] All five have ComponentPreview, code blocks, PropsTable, DoDont
- [ ] Content is in Spanish
</verification>

<success_criteria>

- All tasks completed
- Five display/feedback components documented
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/09-component-docs-complete/09-02-SUMMARY.md`
</output>
